# Sunscar Desert rotation 1 — bounded implementation brief

The selected destination-cleft direction should make a local mineral outing readable while preserving its existing sources and Emberhorn decision. It does not authorize literal target copying or an unmeasured terrain rewrite.

1. **North crystal room:** retain the destination crystal at `(-1934.1,87.2)` as the only hero mineral. Add only support-verified, HUD-safe pocket-edge forms that frame it without narrowing the centre approach.
2. **Rib-and-basin rhythm:** expose the existing shallow dry-rib terrain through one low forward rib and spaced return-side fragments, never a large free-standing monument. Validate final masked heights and normal-input clearance.
3. **Arrival echo:** at the twin arrival crystals, use the same sparse rib-foot/stone vocabulary to keep the basin open while suggesting the circuit's next direction.
4. **Saddle choice:** keep the central iron/Emberhorn route open and the west skirt visibly clearer. Do not move the creature, ore, home disk, or add a danger mechanic.
5. **Edge ecology and return cue:** cluster existing stone/sparse dry vegetation only at rib feet and pocket edges; leave exposed basin centres bare. Use separated east fragments as the return cue after their full mesh projection clears HUD and source footprints.

| Need | Existing system or asset | Admission condition |
| --- | --- | --- |
| Harvest identity | `asset_crystal`, `asset_iron_ore_rock`; finite resource/persistence owners | Existing IDs, state, transformed footprint, and approach clearance remain exact. |
| Small edge dressing | `asset_trail_stones`, `asset_fen_stone` | Render full transformed bounds; require terrain support, no protected overlap, and central portrait visibility. |
| Dry rib silhouette | Audit existing outcrop/rock recipes before declaring a gap | If no matching warm, grounded low rib exists, create one bounded reusable eroded-rib asset only. |
| Route/encounter | Existing terrain, residency, Emberhorn home/roam | Ordinary local route and reload proof; no new state owner. |

Run the structural pass only after the asset audit, numerical placement/support plan, full-geometry projection through the four frozen poses, and protected-footprint/lane checks. The target's deep cleft, many independent crystals, large pale formations, and cinematic density remain aspirational until those gates pass.
