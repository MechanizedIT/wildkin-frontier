# Rootbound restart destination staging amendment

**Status:** frozen before restarted structural implementation. No subsequent pose change is permitted for this pass.

## Amendment

The restarted Destination comparison pose is **`(-449, 675)`**, yaw **`-2.30`**, using the same portrait pitch `0.6283185307` and effective distance `11.5910795442` as the original review camera. It is 10 m from the historical destination pose `(-445, 665)`. Arrival, Gallery, and the overhead diagnostic pose remain unchanged.

## Why the historical pose could not carry the selected composition

At `(-445, 665)`, the left/right staging slots required by the selected Lantern Hollow / Thornstone composition overlapped protected forage clearances. Only one admissible log slot remained, while the existing ring/stone offsets did not form the required normal-camera composition. Recreating the target there would have weakened the existing clearance contract or reduced the target to a lone cue, neither of which is permitted.

The corrected pose was selected before source editing from immutable HEAD captures after a bounded 15 m staging scan. It looks through the existing supported forage context and provides two physically admissible cluster areas while retaining the same circuit and camera configuration.

## Predeclared acceptance criteria

The restarted Destination view must show all of the following through the fixed portrait camera:

1. A sheltered log-and-fungal cluster on one edge;
2. A grouped thornstone counter-edge on the other;
3. A visibly open central lane that preserves forage, wildlife, and player/companion clearance; and
4. Local 1–3 m relief or room framing that makes the grouping read as a pocket.

A bare log, ring, or stone cue alone is not a successful implementation of the selected target. The projected centers below are feasibility diagnostics only, not evidence of visual success: log `(-444, 678)`; rings `(-442, 678)` and `(-442, 680)`; stones `(-448, 679)` and `(-446, 679)`.

## Immutable comparison baseline

Captured before restarted source edits:

- `art/reviews/rootbound-wildwood/restart-baseline-arrival.png`
- `art/reviews/rootbound-wildwood/restart-baseline-gallery.png`
- `art/reviews/rootbound-wildwood/restart-baseline-destination.png`
- `art/reviews/rootbound-wildwood/restart-baseline-overhead.png`
- `art/reviews/rootbound-wildwood/restart-baseline-captures.json`

Restart R1, its up to two focused repairs, and final review use these baseline identities and this exact amended Destination pose. Historical `baseline-*`, `r1-*`, and `r2-*` frames remain evidence for the superseded attempt only.
