# Independent plan review — Lantern log R4

**Verdict: GO for one focused body/end repair.** Reviewed `repair-r4-plan.md` SHA-256 `ea67844829f66aa54d73561cf0fc56dd1b1e8db80232cf754a0bfdd1f9a80e81`.

This is a proportionate response to R3's held gap. It changes the visible culprit—the regular rail body, parallel bark strips, and uniform end—while preserving the successfully readable six-cap hierarchy and its R3 stem/cap geometry. The literal angular depths, longitudinal weights, lower-belly bounds, near/far rim offsets, retained `.58` inner ratio, and surface-based moss refit give the builder an executable shape rather than a cosmetic texture request. The shallow recess stays closed and no passage, runtime, or harvesting behavior is implied.

The preservation boundary is sound but must be proven from generated geometry: the lower-only belly transform should leave the colony's upper substrate largely intact, yet the small 175-degree fissure and new interpolated samples can still alter a support triangle near a stem. Before rendering, compare every R4 stem/cap vertex/index/transform to R3 exactly and repeat all six bounded log-support and cap-underside contacts against the R4 surface. Moss may be refit only by bounded ray hits to the rebuilt log; its named footprint must remain recorded.

The broken near rim and raised far tip make valid end triangulation a material construction risk. Require post-clip/post-translation recalculated normals, nondegenerate triangles, directed-edge closure, positive log ground-plane area, and a retained all-angle R3/R4 matched-frame comparison. The requested shared orthographic bounds fit prevents the revised silhouette from receiving a favorable crop.

This clears one construction attempt only. It does not pre-pass R4 visually or restore runtime admission.
