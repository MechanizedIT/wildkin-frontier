# Independent reference review — Lantern Grove log colony V1

**Verdict: KEEP, conditional for a high-detail master reference; not a literal runtime-geometry brief.**

The image provides a strong, useful ecological read: a single grounded fallen log, an asymmetric near-end cap cluster, one farther cap that carries the eye along the log, and visibly planted stems at bark/crevice contacts. The violet-cap, pale-stem, dark-rootwood, and restrained olive-moss palette gives Lantern Grove an identity without glow or a separate diorama. The uneven cap hierarchy is more useful than a field of repeated forage objects.

It exceeds the intended runtime art language in three ways. Bark plates, cap gills, moss, and splintering are much denser than a mobile derivative needs; the largest cap dominates the log more than the stated 0.42 m relationship suggests; and the broken left end reads as a deep open hollow rather than the requested shallow decayed recess. The single view cannot establish hidden underside, stable contact footprint, or collision behavior.

For construction, preserve only the silhouette hierarchy: one short, slightly kinked grounded log; one broken/chipped end; one hero cap, two medium caps, and three small caps, clustered asymmetrically near that end with a single farther medium cap. Model the log as a mostly solid, stable low-poly mass with a shallow visual recess, not an enterable or collision-relevant pipe hollow. Use broad faceted bark planes, a few moss islands, cap undersides simplified to radial wedges, and firmly embedded stems. Review the fungal cluster separately from any existing forage interaction; the reference neither creates a new item nor proves that every cap is harvestable.

Before a runtime derivative is considered, provide an explicit scale/collider plan, underside and end-face views, 48/96px renders, and a measured Grove placement. No reference revision is required now: the visible excess detail is a clear derivative constraint rather than an ambiguous design failure.

## One guarded generation method

**GO for one unchanged full-export fresh-process TRELLIS 512 attempt** using this reference, 12 steps, seed 1234, 30,000 requested export faces, 1K texture, the normal 750,000 decoded-face refusal, and existing stage/6 GiB reserve gates. This is a suitable high-detail reference for that single method. It must not use the Rootbound-buttress geometry-only exception, relax a guard, or convert a resulting export into runtime admission. If it fails the decoded-face guard or produces the same over-noisy/hollow-first reading, retain the result and use one specific reference repair rather than vague repeated prompting.

## Focused reference V2

**Selected for the one guarded full-export attempt: `reference-v2.png`, SHA-256 `85bd175a6b102cf183b50ff5ca45a1afd6847a6920a28b0039bd8e1e98d6b2ac`.** V2 keeps the useful asymmetric six-cap colony, visibly planted stems, short grounded log, and violet/pale/brown/olive hierarchy while removing V1's distracting micro-bark, gills, and surface noise. The remaining near-end darkness must remain a shallow visual indentation in any derivative, never a tunnel or automatic collision void. The ordinary 512/12/seed1234/30k/1K full-export method and its unchanged 750k decoded-face/6 GiB guards are GO for exactly one attempt.
