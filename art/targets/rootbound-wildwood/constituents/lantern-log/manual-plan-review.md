# Independent review — Lantern log manual fallback

**Verdict: GO as a contingent manual construction plan only if the second guarded TRELLIS full-export run repeats its decoded-face refusal.** It is a changed-method third candidate, not a fallback that can run in parallel with, or substitute for, the guarded run.

The plan is practical and visually targeted. Literal 12-sided log sections produce a short, gently bent grounded volume; the measured centerline changes are modest and coherent. The six mushroom centerlines create a clear hero/medium/young hierarchy and retain the farther cap. The CPU probe genuinely triangulates the 96 log side faces and measures all six stem-center penetrations (1.37–6.49 cm) plus cap-axis overlaps (1.36–4.30 cm). Its stated limits are honest: those results do not stand in for actual cap-rim coverage, end closure, underside contact, visibility, or a runtime fit.

The method correctly uses closed solids, broad planes, a shallow recessed end rather than a tunnel, and intentionally documented overlap rather than a Boolean or noisy high-detail surface. It preserves the need for all-angle, 96/48px, full-envelope, outward-normal, and actual contact checks before any result is considered.

Use the frozen parameters SHA-256 `6baec8142f7c792b289d1c09887a7a0825a77b959f180661f364ac6c44b47988` and retain the probe receipt SHA-256 `f8c80090cb2b7f4b1d57835d655a85ba04f214408c2e4168e4958ae938f15c34`. Before builder execution, make the end annulus/recess construction explicit in the executed source and ensure the complete candidate audit derives its bounds from final generated vertices, not section labels. Those are required source-to-plan checks, not another plan revision.

No GLB, placement, collision, harvesting, regrowth, or runtime admission is authorized by this GO.

### Grounding correction

**GO.** The revised parameter set clips the actual generated log outer/inner/end vertices to source Z=0.075 m and translates the completed asset by −0.075 m as one unit. It does not alter stem/cap relationships or substitute a point contact for support. The updated CPU receipt retains the six limited contact measurements and reports 16 source-ground-plane triangles, while reserving the required final positive-area ground assertion for the generated mesh. Reviewed parameter SHA-256: `e65df488e76b4c6c4c47c134fcb123c6dfd8c6512c1f99471d8fe982b9486043`; receipt SHA-256: `55976937b71824472274056be9856bdc97fada38c77bcf2671bb8d31291d20fe`.
