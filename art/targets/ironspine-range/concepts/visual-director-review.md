# Ironspine Range — visual director review

**Selected reference: `selected.png`**, an exact copy of
`gully-arrival-direction.png` (SHA-256
`e93b0f4183ef2abe13a6ca96bd2454dfb4d1dc9b936f75122dc237b878a7b26c`). It
is the stronger direction because it gives the circuit an immediate, legible
identity: a sheltered mineral gully with a broad walkable centre, a visible
lower ore stop, and a rising outlet. It also complements the real high-shelf
baseline rather than asking that shelf alone to carry the whole outing.

The target is a useful portrait composition reference (**7.7/10**), retaining
the captured HUD, player scale and an open lane. It is not a runtime claim.
Its tall, continuous stratified sidewalls, dense edge dressing and detailed
ore surround are not presently admitted by the numerical survey. The crown
image contributes a secondary idea only: a sparse exposed shelf and return
view, not a second primary target.

The current matching native poses remain locked: arrival `(-2080,-3160)`,
yaw `-2.783`; interior `(-2095,-3080)`, yaw `2.622`; destination
`(-2140,-3005)`, yaw `-0.733`; plus the recorded native overhead. Use their
actual captured camera matrices/HUD rectangles, never an inferred yaw or
camera reset. `lattice-path-corridor-audit-r2.json` is a better analytical
route than the original: it supplies a 485.982m full ordered lattice with no
listed protected-envelope intersection, but remains unproven normal-input
traversal.

A target-defining gap is a **continuous, grounded, stratified gully-sidewall
form**. The current admitted `asset_trail_stones`, `asset_fen_stone`, mushroom
rings and verge canopies can supply edge texture only; their counts do not
prove a coherent wall or shoulder. First render/audit their transformed
silhouettes against the locked views. If they cannot make the sidewall through
bounded terrain support, one separately reviewed Ironspine rock/landform asset
may be considered. This review does not admit one.

No literal image reproduction, climbing/hazard feature, new mineral behavior,
resource move, wildlife encounter, lighting/camera change, broad allocation
rewrite, or return-route invention is authorized by the selected direction.
