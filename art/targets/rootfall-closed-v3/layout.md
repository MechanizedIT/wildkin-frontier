# Rootfall closed V3 — geometric removal contract

Status: provisional reference-author contract, pending independent CLOSED target review. This document defines measurable intended geometry; generated pixels are not survey measurements. Read with target.png and the unchanged native baseline receipt. V1/V2 remain held studies.

## Frame, placement and retained terrain

World axes: X across the passage, Y up, Z along the approach. Proposed assembly origin (0,1.35,-31), yaw0; local coordinates below use that origin. Existing ground is a raised ridge with a broad top, not a depression. Keep its terrain and collision surface. Approximate total assembly envelope: local X[-7,7], Z[-2.5,2.5], Y[0,3]. This is a maximum visible asset envelope, NOT one solid box collider. Individual root and rock colliders must fit the visible grounded forms. No large invisible flank wall, new drainage cut, tall near-camera canopy or raised floor bridge.

Preserve gate id gate_section_1_to_2 at (0,1.35,-34), yaw0, its destinations/return relationship and entry IDs. Parent owns gate mechanics. Its current arch visual is replaced; this contract does not move the destination or trigger. The existing inbound return landing is (0,1.35,-31.5), inside the center opening. It must be collision-safe after repair, including player body clearance and settling.

## One trunk, two immutable planes

Use one authored coherent assembly with permanent left, removable center and permanent right pieces. The two cut planes are local/world X=-1.65 and+1.65. The removable piece is exactly the3.3m interval between them. CLOSED and OPEN share the same surviving vertices/transforms, outside bark, roots, branch fingers, bank rocks and foliage. No shortening retained ends, moving banks or stretching the gap during opening.

CLOSED center reaches the visible lane floor and blocks the route. At each plane show only a shallow irregular front-facing starter notch: maximum roughly0.3m vertical extent and0.1–0.2m inward bite, dependent on actual trunk contour. Keep continuous exterior bark above, below, at the back and around the intact core. Exposed sapwood is muted warm tan. No full circumferential pale ring. The generated image still suggests broad bands; that is an author-flagged defect, not permission to model girdles.

OPEN is derived by removing ONLY the center interval and exposing end faces on those same exact planes. No independently generated second tree is used as an OPEN target. The center cuts and the end caps must originate from the same plane coordinates. Frozen left/right object transforms and topology provide objective continuity evidence.

## Clear lane and four-wood/two-fiber brace

Reserve world X[-1.5,1.5], Z[-35,-28] as the minimum3m-wide passage, with sufficient overhead/player capsule clearance. The0.15m margin on each inner end helps keep visual splinters and collision out of this lane; test actual capsule/slope traversal rather than assuming it is sufficient. No root, rock, timber, foliage collision or dangling cut fragment enters this corridor in repaired OPEN state. Keep the return landing above clear ground.

Two near-side support pairs, one on each bank, total FOUR recognizable timber members and TWO visible fiber lashings, one lashing per pair. All support geometry and colliders stay beyond X±1.5. Each individual timber has a grounded foot and a direct upper bearing/contact against retained wood or the paired member at a supported joint. No suspended beam, ornamental crossing, brace spanning the lane or unexplained connector.

Provisional support zones: left X[-3.2,-1.8], right X[1.8,3.2], world Z[-31.5,-29]. Pick final foot and bearing points from the actual retained contours/terrain. These are bounding zones, not permission for floating contacts or four coincident poles. Repaired supports use existing matte wood/fiber vocabulary. The4wood/2fiber resource cost is implemented by the parent; no XP threshold.

## Required proof before admission

After the CLOSED reference independently passes, production must export fresh CLOSED and derived OPEN views under the SAME locked native-style camera. Judge biological identity against this single CLOSED reference, then judge continuity, fixed cut planes, exactly removed center, stable bank/root/branch geometry, supported brace joints and lane width against this contract. A render pair alone does not prove runtime collision.

Runtime validation additionally covers approach targeting from the current raised ridge, blocking before clearing, actual Omni clearing, any cut-but-unbraced intermediate state, resource repair, save/reload of existing repaired state, gate travel and safe return landing. An intermediate visibly open lane must not retain an invisible solid obstruction. The parent selects a simple consistent intermediate behavior using the existing owners; this reference creates no persistent ledger or generic framework.

Native camera defaults and player scale remain unchanged. Baseline capture used diagnostic player placement at (0,terrain+.55,-26), an844x390 DPR1 viewport and settled default game camera. It did not alter camera/world defaults or use Author mode. The generated image is a design target, not an in-game screenshot or evidence that any state was implemented.
