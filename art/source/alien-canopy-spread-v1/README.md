# Spread alien canopy — model and native review pass

Exact shipping candidate: assets/models/alien-canopy-spread-v1/model.glb.
SHA256: d6351470e06227f6f8dc14c768a19965fdc953b5fc4c1473102cc22e2afdbae3.
Stable library ID: asset_verge_canopy_spread.

Independent reference PASS 8.2 and model PASS 8.0 by /root/level_design_review. Root authored the reference; fabricator_model implemented the model. Current manifest.json and asset.json record model/native PASS 8.0 and separate packaged PASS. Native reverse/headroom/fade/sibling/Author evidence is retained under native/. Physical-phone acceptance remains pending. generation.json is deliberately preserved as the historical build receipt, which predates model review. Original V1 HOLD and V2 study remain intact under .dream-loop/overnight-canopy-variants.

The V2 correction gives the same twelve closed paddles substantial convex middles and rolled shoulders with unequal fan planes. Primary trunk/forks and reference remained fixed. Rear crown remains shallower and darker than the target; it now shows individual bellies, faces and gaps. Native reverse views must confirm it does not collapse into a flat strip. Broad foreground fade/restoration, under-crown Explorer visibility, sibling isolation and owner phone appearance remain separate gates. No global lighting change is implied.

Editable source: blend.blend contains 35 named parts with packed palette. builder.py is the exact V2 build helper; raw.glb is the exact reviewed export. optimization.json/asset.json hold structural checker results. normalization.json/export-clearance.json hold actual imported triangle surfaces and footprint-plane edge intersections. review-front.png is front3q; review-rear.png, front.png, side.png and underside.png retain all five actual model views. render-evidence.json records their original filenames and cameras; its pending text is historical rendering status, not current model admission. Contact coordinates in generation.json are pre-normalized construction evidence; saved Blender/GLB owns final coordinates.

Cost: 3,100 triangles, 5,884 exported vertices, 2 batches, 1 embedded 256px palette, 211,172-byte shipping GLB. No bones or animations. Bounds: 5.55m wide x 3.82m high x 3.65m deep; grounded Y0, +Z forward. Actual minimum surface outside the 1.08m central footprint: 2.580727m; at minimum 0.9 scale: 2.322654m. Both exceed required 2.45m / 2.205m. Numeric checks do not establish native appearance or phone performance.

Integration: preserve all eight authored transforms/flags, scales0.9-1.5 and explicit collision:null. Do not inherit the standard canopy collider. Descriptor scale1 and pivot{x:0,y:0,z:0}; placements apply their existing scale. Two model batches versus one merged primitive-recipe batch is a real cost increase, not a43-part-to2 runtime saving. Standard/tall canopies and harvestables remain separate.

Rebuild using Blender4.5 background --threads 4 --python builder.py -- --output-dir <fresh-study-directory>. Existing export destinations are refused. It produces a fresh isolated GLB import, exact surface audit and five512px20sample CPU views. No subdivision, heavy modifiers, GPU inference or runtime network request. Packaging made no registration/world/runtime edits. Only model.glb is shipping data; source/target files stay outside the game ZIP.
