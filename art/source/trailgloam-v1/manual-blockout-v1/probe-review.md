# Trailgloam cuff-junction probe — independent review

**Decision: PASS for the named junction method and GO to initial Gate-A neutral massing only.** This is not a creature, export, runtime, rig, collider, or visual-admission decision.

The actual probe is a single exact-union mesh: `82` vertices, `160` triangles, zero non-two-face edges, zero reported non-manifold edges, and no validation issue. The construction record uses Blender Z-up consistently and makes two six-sided cuff volumes overlap the shell patch before the native exact union. The recorded cuff centres are 0.34 m apart against two 0.115 m radii, leaving a 0.11 m radial gap; the limbs leave in different planes (51.6°). That is a usable local method rather than three children copied into one shared shell ring.

The underside render visibly resolves two separated cuff/limb exits under one closed shell patch; the side render shows planted, non-coplanar stub directions. The top render is necessarily dominated by the opaque shell patch, so it does not independently prove the underside seam; it does not contradict the measured topology.

For Gate A, repeat this exact manifold/normals audit on the complete shell after every pair is attached, and make the six resulting socket-to-sole paths readable in the prescribed top, front, rear, both-side, underside, and three-quarter evidence. Manifold counts alone do not prove a six-legged creature, prevent all self-intersections, or pass the later 48/96-pixel, material, collider, export, motion, or runtime gates.
