# Trailgloam manual blockout â€” R2 construction-method amendment

**Status: PROPOSED R2 method only.** This is the counted focused repair after the independent Gate-A HOLD 3.0. It replaces the sphere/Boolean/voxel recipe; it does not authorize a Blender run until an independent plan review records a decision on the envelope conflict below. Initial files, including `build_massing.py`, are retained as historical evidence.

## Evidence that drives the replacement

The construction sheet `../multiview/trailgloam-orthographic-v1.png` shows a broad, shallow beetle with a restrained forward wedge, a dark underside rim, three separated root intervals on each side, and two amber **folded blades** on dark posterior sockets. The initial front, left, top, underside, and 48/96 views instead show a tall spider/horn silhouette; its receipt reports two vertex components and localizes the second to the RR hoof assembly. R2 therefore uses no broad spherical belly/head and no Boolean-plus-voxel cleanup as its structural method.

## R2 mesh recipe in frozen Blender axes

Use metres, `Z=0` ground, `-Y` head-forward, `+X` right, `+Z` dorsal. Retain the source-backed six chain anchors and three-part sequence from `manual-anatomy-plan.md`; the only hidden-side adjustment remains roll/depth for nonintersection.

1. **Shell and head aperture:** create the same closed 12-sided section loft and dark `.43-.54` belly rim, but author the forward lower shell patch with one named five-vertex aperture `H0..H4`, in counter-clockwise order when viewed from the shell exterior. The small five-face head wedge has an uncapped neck loop `N0..N4`, also exterior-counter-clockwise. Bridge only the two open boundaries with the explicit reversed mapping `quad_i=(H_i,H_(i+1),N_(4-(i+1)),N_(4-i))`, modulo 5. The head has no overlapping closed neck cap and no Boolean/voxel cleanup.
2. **Cuff-to-hoof chains:** preserve each frozen `LF/LM/LR/RF/RM/RR` centreline, but use six vertices at **every attachment boundary**: shell aperture `S0..S5`, cuff shell/end loops `C0..C5` and `D0..D5`, upper start/end `U0..U5` and `V0..V5`, lower start/end `W0..W5` and `X0..X5`, ankle `A0..A5`, and flattened hoof neck/perimeter `P0..P5` and `Q0..Q5`. The visible upper/lower forms remain tapered prisms and the hoof remains a broad flattened wedge by moving these six perimeter points into a wedge profile; it is not a four-sided closed cube. Every pair is bridged with six quads and the same reversed-loop rule `quad_i=(first_i,first_(i+1),second_(5-(i+1)),second_(5-i))`, modulo 6. Attachment loops are uncapped; only the shell top/bottom and the ground-facing hoof sole are capped. This gives RR the same real `S -> C -> D -> U -> V -> W -> X -> A -> P -> Q` surface path as every other leg, with no detached contact object.
3. **Frond socket holes and folded blades:** pre-author two distinct posterior hexagonal apertures in the shell surface, `FL0..FL5` and `FR0..FR5`, exterior-counter-clockwise, centered at `(-.23,.22,1.00)` and `(.23,.22,1.00)`. Their outer radii are `.11 m`, so the `.46 m` centre distance leaves `.24 m` before shell curvature and exceeds the required `.10 m` edge gap. Each dark collar has an uncapped lower loop `CL0..CL5`/`CR0..CR5`; bridge its aperture with the reversed six-loop rule above before joining the closed thin folded prism. Base/mid/tip blade sections remain `.23x.10`, `.34x.12`, `.08x.08` with the `.04 m` inward fold ridge and the frozen `1.32`/`1.55` centreline. No collar is an overlapped closed solid and no shell face is deleted after creation; the shell patch is generated with these three apertures and their boundary loops from the start.
4. **Native operation and invariants:** use Blender 4.5 Edit Mode/BMesh mesh construction with authored boundary loops and `bmesh.ops.bridge_loops` (or the equivalent Bridge Edge Loops operator on only the named open boundary edges). Before every bridge, assert equal loop count, no duplicate vertex indices, each loop is one closed boundary cycle, and no loop edge has a face on its attachment side. Reverse the second loop ordering before bridging, create exactly one quad per corresponding edge, then recalculate outward normals. These are the native mesh-edit/bridge operations identified by the applicable Blender guidance; no arbitrary face deletion, overlapping closed volumes, self-intersecting surface, Boolean union, or voxel remesh is permitted.
5. **Neutral only:** no eyes, lantern seam, textures, rig, motion, GLB, collider, runtime, or export.

## Required mesh facts before rendering

The builder must produce these from the resulting mesh, rather than infer them from object names:

- `mesh.validate()` reports no repair; every edge has exactly two incident faces; exactly **one** vertex-connected component.
- A triangle-BVH self-overlap check, excluding identical or edge-adjacent triangles, reports zero intersections. Recalculate outward normals and record the check result.
- The actual contact graph contains six shellâ†’cuffâ†’upperâ†’lowerâ†’ankleâ†’hoof paths, including RR, and two shellâ†’collarâ†’fold-baseâ†’fold-tip paths. Each hoof has `min Z = 0` and no sole penetrates below ground.
- Same-side socket centre distances are `LFâ€“LM â‰ˆ .371 m` and `LMâ€“LR â‰ˆ .393 m`; with `.10 m` cuff radii this preserves at least `.171 m` and `.193 m` of visible root gap before modelling thickness. Top/underside renders must show all six distinct root-to-sole paths; no leg may close the LF/LM, LM/LR, RF/RM, or RM/RR gaps.
- Re-render front, rear, left, right, top, underside, three-quarter, and native 96/48 px after those checks. The 48 px frame must still read as a low saucer with two folded blades, rather than a spider/horn silhouette.

## Envelope reconciliation requiring review

R2 proposes a revised **anchor-derived aggregate envelope of `1.88 X x 1.89 Y x 1.70 Z m`**. The calculation is explicit: LM/RM hoof centres are `+/- .78 X` and their `.32 m` hoof width adds `.16 m` on each outside edge, giving `1.88 m` X; the fore hoof centre at `Y=-.86` with `.25 m` depth reaches `-.985`, while the rear centre at `.78` reaches `.905`, giving `1.89 m` Y. `Z=0` soles through the planned `1.55 m` frond tip retain `.15 m` vertical allowance inside the existing `1.70 m` headline.

The earlier `1.45 x 1.55 x 1.70 m` headline therefore cannot include the frozen feet. It was explicitly a proposed build target rather than a recovered construction-sheet measurement; the opaque sheet establishes proportions and six-root layout but no metre scale. R2 retains the more specific frozen anchors and widths rather than silently scaling or axis-converting them. The visual control is instead the low `1.16 x 1.00 m` shell and shallow section-loft profile: the `1.89 m` planted-foot spread is about `1.63 times` shell width and `1.89 times` shell depth, which must be judged against the all-angle sheet rather than relabelled as a `1.45 x 1.55 m` result.

**Review request:** approve or reject this explicit envelope revision together with the section-loft/explicit-loop method. If the revised footprint is not acceptable, the reviewer must direct a new set of leg centreline/hoof dimensions before R2; do not proceed with contradictory measurements.
