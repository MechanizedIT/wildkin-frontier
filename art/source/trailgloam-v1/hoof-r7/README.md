**PAUSED before execution by latest owner TRELLIS-first steering.** Prepared source preserved; no R7 Blender model or renders exist.

# Trailgloam R7 hoof-only renderer

This source is prepared for independent review; it has not run Blender. It imports the exact R6 CPU construction and the approved R7 cuff-derived pad routine, replacing only the six `*_hoof` parts. All non-hoof R6 literal vertices and indices are signature-checked before Blender object creation.

The source pins the approved R7 CPU routine and proof hashes. After independent source review, root may run the guarded command with a new absent directory:

```powershell
& 'C:/Program Files/Blender Foundation/Blender 4.5/blender.exe' --background --factory-startup --python-exit-code 1 --python art/source/trailgloam-v1/hoof-r7/render_hoof_r7.py -- --execute --plan-dir C:/Users/cwood/Documents/mobile-rpg/art/source/trailgloam-v1/hoof-r7-plan --output-dir C:/Users/cwood/Documents/mobile-rpg/art/source/trailgloam-v1/hoof-r7/candidate-r7
```

The renderer requires 8 GiB free RAM before imports and terminates its owned run if free RAM drops below 6 GiB. It produces seven neutral views at 512, 96, and 48 pixels, plus an editable Blend and receipt. It does not run unless `--execute` is supplied, and it does not touch runtime, export, rigging, or existing R6 artifacts.
