# Rootbound buttress study — independent construction-plan review v2

**Reviewed inputs:** corrected `construction-plan.md`, corrected `reference-manifest.json`, `root-image-measurement.json`, the original `../../rootbound-buttress-v1/reference/target-v1.png`, and the approved construction-reference contract.

## Decision: PASS — shared fresh-builder plan may begin

The three required corrections are present and correctly bounded.

1. The plan now cites numerical foreground bounds of 1,149 px wide by 1,198–1,199 px high (projected ratio 1.0426–1.0435) as **screen-space corroboration only**. It no longer treats that ratio as recovered 3D geometry, and its 4.8–5.8 m source-facing spread guidance is compatible with a 5.6 m asset inside the <=7 m footprint.
2. The fork-window requirement is now the observed normalized envelope `x≈0.38–0.65`, `y≈0.39–0.58` under a builder-declared matching review framing. This removes the prior unsupported meter projection while preserving an independently checkable opening.
3. The source-facing massing gate now asks for a continuous grounded base and several readable toes. It requires all five closed buttresses across the rotating all-angle evidence, which avoids forcing an artificial radial fan into the target view.

The plan remains adequate and measurable for a fresh volumetric build: 5.0–6.0 m height, <=7 m lateral envelope, <=5,000 triangles, one opaque matte material, explicit connected root/trunk/fork operations, non-flat canopy depth requirements, and all-angle plus game-scale gates. It clearly distinguishes observed front-view evidence from inferred rear/depth topology and prohibits reuse of the held candidates.

## Builder conditions that remain binding

Start with untextured massing only. The first reviewer gate needs the actual fresh mesh from front, rear, both sides, and three-quarter views before palette/detail/export. Use the declared source-facing review framing when evaluating normalized landmarks; do not claim camera recovery from the target. Preserve the locked root/canopy route and runtime constraints. This PASS authorizes construction-plan use only; it does not admit an asset, collider, GLB, or runtime placement.
