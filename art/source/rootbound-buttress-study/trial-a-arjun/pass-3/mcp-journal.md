# Trial A pass 3 — focused canopy repair record

This was the final authorized Trial A repair. It changed only the six existing canopy meshes and their existing `Col` vertex colors in the live Blender source; wood topology, material count, budget, and camera were retained.

For each canopy mesh, the builder displaced existing vertices with angular oblique-plane tilt, asymmetric downward lobes, and small lateral variation. It repainted the existing one-material palette by structural plane: dark underside/interior, moss midplanes, and restrained yellow-green upward facets. No faces, materials, texture images, leaf cards, collider, or runtime data were added.

The fresh source and exact output are `rootbound-buttress-trial-a-final-repair.blend` and `rootbound-buttress-trial-a-repair.glb`; final all-angle and 48/96 evidence is under `final-renders/`. `glb-check.json` records the structural PASS at 4,542 triangles and one material.

This is a factual operation record, not a literal recovery of the original MCP Python payload. The editable Blend and the final GLB are retained as the authoritative executable artifacts. The independent final review holds the result at 7.4/10, so no fourth Trial A repair or admission follows.
