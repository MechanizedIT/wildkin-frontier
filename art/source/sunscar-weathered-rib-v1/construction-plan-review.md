# Sunscar weathered rib V1 — independent construction-plan review

**Decision: HOLD for one small executable-table clarification.** The frozen `geometry-plan.json` hash (`67012e6…`) supplies a credible continuous seven-station, eight-vertex closed loft: its actual extremes fit the stated 4.4 × 2.1 × 1.6 m envelope, both end stations can be capped, and the two bottom perimeter points per ring are at Z=0. Station 1's 1.60 m shoulder followed by the 1.46 → 0.84 m crown sequence is a specific route to the target's raised left shoulder and descending long cap; it is not a generic box. The stated one-material/≤700-triangle budget and cheap pre-render closure/component/grounding audit are suitable for this known routine loft method. A separate whole-method probe is unnecessary.

The plan does not yet make the visual-detail operations deterministic enough for a separate builder. In `connectedTerraces`, `hostStations: [1,4]` and `[0,5]` can mean either two isolated stations or inclusive station ranges, while `hostLoopBand: [2,5]` and `[1,3]` do not name the exact quad strips or the new shared loop/vertex sequence. `erosion_gullies` similarly names station indices but no perimeter indices, target face strips, or split order. That ambiguity permits a generic continuous loft with superficial or disconnected detail, which would miss the target's layered cap, front bedding shelf, and shallow vertical gullies.

Before execution, amend only the machine table to define for each operation: inclusive station interval, exact existing ring-index spans and corresponding quad strips, ordered inset/split/extrude sequence, permitted offsets, and the expected shared-vertex attachment to the original shell. State whether the two end caps are generated before or after all inset work and require their final closure audit. Keep the current envelope, station values, one-mesh topology, material budget, and review views unchanged. Once those references are explicit, **GO for the initial massing build only**; export, collider, placement, and runtime admission remain separate gates.

## V2 deterministic-detail recheck

**GO for the initial massing build only.** V2 resolves the held execution ambiguity: `R`, `Q`, and each named centre fan now have fixed inclusive station/loop ranges, winding, offsets, shared host-ring vertices, post-detail cap order, and an envelope proof. The continuous loft and its 1.60 m left shoulder followed by a 1.46 → 0.84 m descending crown remain credible for the target's main broad mass.

The centre-fan operations are structurally safe but visually limited. Because every new centre moves inward/downward inside its host quad, they make faceted recesses or dimples; they do **not** by themselves create a broad projecting stepped ledge. The target-facing massing must therefore visibly show a continuous high left shoulder, long descending cap, and at least one broad proud/recessed bedding transition in the source three-quarter and side views. If the result reads as a smooth-ish loft covered in local dimples, HOLD it and use the focused repair to make one connected bounded strip with a real inset/extruded perimeter loop, retaining the single closed shell. Do not mistake manifold centre fans for stratified target likeness.

This clears neither final visual admission nor export, collider, placement, or runtime work.

## R2 stepped-ring repair plan recheck

**GO for the one focused R2 massing repair.** `geometry-plan-r2.json` SHA-256 `d6bd4f21…` replaces the failed centre-fan method with a simple, ordered 11-point cross-section: both shelf pairs have nonzero Y span and share their boundary edges with the adjoining rises. The section order moves monotonically from front bottom through the two shelves to the rear and back to rear bottom, so the declared polygon is a credible simple closed contour rather than an intersecting fold. Across stations, its extrema remain within X ±2.2 m, Y ±1.05 m, and Z 0–1.60 m.

The profile values materially change the target-facing mass: stations 0–2 carry the 1.30/1.60/1.46 m shoulder; the lower shelf continues from 0.96 to 0.64 m across stations 2–6; the upper-to-lower transitions make actual stepped planes instead of recessed fan facets. The ends can be triangulated only after the six bridges; the specified shared-boundary/normal/BVH audit is appropriate. `manifest-r2.json` references the same frozen repair hash and preserves V1/V2 provenance.

At Gate A, HOLD the result if the source three-quarter still reads as one smooth roof slope, if the side shelves have zero visible width, or if front/rear remain equal generic prisms. The focused correction has to create the broad step bands in the outer contour; counts, end caps, and manifold closure alone cannot satisfy it. Export, collider, placement, and runtime admission remain outside this GO.

## R3 final-plan numerical recheck

**HOLD for one direct table/doc correction before the R3 source script.** The cap method is now appropriate: create each concave cap as its own existing ngon, apply `bmesh.ops.triangulate` to those faces, recalculate normals, then validate projected coverage and a cap-facing opaque diagnostic after numeric gates but before studio views. This directly replaces R2's failed delete-then-`triangle_fill` sequence. The 12-point profile, variable shelf depths, tapering termination, and stated narrow-phase requirement are also a credible final repair method.

The plan's long-cap sentence is numerically false as written. It says the visible lower shelf descends **0.93 → 0.55 m**, but the coordinate rule puts that shelf at `.68 * L`, giving **0.632 → 0.374 m**. That lowers the intended broad band too far and makes the target-facing profile ambiguous. Prefer the stated, target-appropriate actual shelf heights: set the lower-shelf outer/inner Z to `L`, and define `frontBreak` explicitly above it, for example `min(U - 0.04, L + 0.08)` (the final station remains 0.63 m, below U=0.67 m). Update the prose, geometry JSON, expected bounds/area inputs, and frozen hash together. Preserve the prior version. Once this one consistent amendment is frozen, **GO for independent R3 source review before the last Blender run**.
