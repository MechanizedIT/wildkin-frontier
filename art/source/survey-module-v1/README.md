# Survey Module — locally integrated V2

**Seven V2 assets are integrated in the local runtime at the exact reviewed hashes.** Independent neutral-model and bounded native gates both PASS8.0; the fixed target remains PASS8.2. Their complete reviews are retained in `candidate-v2/independent-model-review-v2.md` and `candidate-v2/independent-native-review.md`. This is local bounded admission, not owner aesthetic acceptance, physical-phone performance, a complete earned survey-to-fitting journey, or full-campaign acceptance.

The folder name `candidate-v2` remains stable for the composer and provenance. Its original `manifest.json` is the frozen model-production receipt; its candidate status is historical. Current integration and exact retained-file/source mappings are recorded in `admission-recovery.json`. V1 retains its HOLD6.3 review and a rejected comparison image.

## Exact exports

Each component has one opaque matte palette material/primitive and embedded 64x8 PNG. Every component passes the unchanged prop structural checker.

| Component | Triangles | Bytes | SHA256 |
|---|---:|---:|---|
| survey-left-wall | 1912 | 141564 | `8b0724eeb2b6c94ec36da6564bada5fbd23be75bc0e534c811c0ee1d364912f4` |
| survey-right-wall | 2448 | 178960 | `446290df32dc148063fffacf373e7ec75ff580a209307ed8372e0d55378ff030` |
| survey-rear-wall | 580 | 45152 | `7b01df4ec586c03e1dc2a2c852df8d105309f7cbf90a87ebafd0720de521fed2` |
| survey-floor | 484 | 38348 | `b8aa63ff73d34be713392600f940403cdafbfb6a49e6f22c8fd5de81bf2f7c53` |
| survey-ramp | 12 | 2532 | `cfd69b2cdac08969859fde1ecadeba86e3b677ee3693cd3e83a7f128bcc53c1e` |
| survey-panel-debris | 136 | 10608 | `578d0a0daca5c24a7ffaaed914a62f000dc69d303a19b7cad9f110fac9b7a82e` |
| survey-cargo-frame | 748 | 58436 | `49e80161b89d545607b444398000660bdd1433400f16d217f80d047243c84926` |

Five-piece shell: 5,436 triangles. All seven components: 6,320 triangles / 7 draws / 475,600 GLB bytes. Exact source bounds, structural checker receipts, hashes and collider data live in `candidate-v2/manifest.json`.

## Structural revision

Continuous layered cabin ribs now have a .7m structural depth, broad ivory upright/shoulder sleeves and large floor sockets. Three paired stations remain, with different broken terminations and one absent armor sleeve exposing its blue core. Hull sheets are thick, notched and divided; the rear is two broken plates and the floor six broad plates on a low continuous backing. A seated receiver support and single visible horizontal axle replace the small mount. The two breadcrumbs are a torn, creased hull sheet and a crushed frame with its front upper rail interrupted. No tiny bolt/noise detailing was added.

## Placement and collision handoff

The five shell exports share module-center ground origin [0,0,0]; local +Z entrance, X width, Y up. World yaw -PI/2 turns entrance west. Floor remains6x7m at Y0â€“.18; ramp remains3.2m wide, Z3.5â€“4.8, descending from .18 to .025. Each breadcrumb retains its own original cargo-footprint ground origin; collider centers account for the small damaged-bound asymmetry.

Main side wall boxes are centered [Â±2.79,1.17,0], size [.68,1.98,7]. The manifest contains exact groundSockets bounds for separate low boxes and upperStructuralHulls arrays of actual transformed source vertices above Y1.91 for optional small upper collision hulls. Those point sets cover upper remnants only; never extend them down to the floor or use a single convex/enclosure collider around the cabin. Rear wall and attached bracket have separate box recommendations. Breadcrumb boxes use measured export bounds including the bent wing. Physics and Author implementation remain parent-owned.

## Fresh GLB views and mechanics boundary

All six current images are in `.dream-loop/overnight2-survey-model/v2/`: front, rear, three-quarter, chest-open-clearance, game-scale-neutral and breadcrumbs. Every view imports the exact exports. The admitted field chest is inserted separately at [0,.18,-1.48], yaw0; no duplicate chest geometry ships in this family. ChestLidPivot is posed -100degrees for the static open-clearance image. The amber backplate remains an attached service bracket, not a second cartridge transaction.

The 844x390 neutral scale fixture includes Explorer v2 and the current52-degree vertical FOV / .85 landscape zoom. Highest rear parts crop at ordinary approach framing; the complete neutral angles show their geometry. These are static Blender fixture images, not native camera, terrain, Rapier, opening-cycle or physical-phone proof.

One owned Blender process used two CPU threads and12samples. Free RAM before V2 was about16.1GiB; no TRELLIS/GPU inference or heavyweight processing. Builder, editable component Blends, palette and receipt remain reproducible sources. The seven source exports now match `assets/models/survey-*-v2/model.glb` exactly. The root completed888tests, world/campaign checks, build/validation and ZIP at this checkpoint; the source-preservation task did not rerun them. Reported package size is43.44MB unpacked /20.37MB ZIP. Publication and physical-phone acceptance are separate.


## Compact recovery evidence

`candidate-v2/evidence/neutral/` retains all six reviewed V2 renders unchanged. `evidence/native/root/` retains actual wreck entry, opened chest and the cartridge-in-pack fitting instruction. `evidence/native/independent/inside-quarter-two.png` preserves a different ordinary orbit with the player/open chest visible. `evidence/receipts/` retains the neutral judge JSON, root earned-route/reload, independent native and supplemental grounding, exact source hashes, diagnostic station fitting, and Author item/export receipts. Full provenance and byte hashes for every copied artifact are in `admission-recovery.json`.

The independent primary native run used ordinary Camp travel, walking/orbit and chest interaction without teleport/material grants. Its tree-harvest attempt was initially outside reach; the supplemental grounding/tree receipt explicitly uses position seeds and proves the corrected close-range harvest. The root's earned cartridge route/literal reload and its diagnostic placed-bench fitting/reload are separate runs. Retaining both does not combine them into an earned one-save end-to-end journey. Native reviews were local dev-server checks; the aggregate portable build/ZIP is a different proof boundary.

Relevant exact harnesses are retained in `candidate-v2/evidence/harnesses/`. Run from the repository root with its existing Playwright dependency, Edge and the local dev server on8080. These preserved scripts write to their original ignored `.dream-loop/overnight2-survey/` paths; create that folder and its `review/` child first. For example: `node art/source/survey-module-v1/candidate-v2/evidence/harnesses/root-wreck-native.mjs`. Each harness creates/closes its own isolated browser. The station harness deliberately seeds construction/materials; the supplemental tree harness deliberately seeds position. The Author harness regenerates its large export fixture, which is not copied into this compact recovery set.

V1 recovery intentionally retains only its builder, original manifest, palette, HOLD review and `rejected-three-quarter.png`. Its original GLBs/Blends remain untouched as local untracked experiments. No browser profiles, dependencies, whole proof dump, large Author fixture, aggregate logs or duplicate review Blend were copied. `recovery-files.json` is the exact selective source/target commit list; it excludes those V1 experiments.
