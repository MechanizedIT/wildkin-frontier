# Intact alien scenery canopy

Exact admitted model SHA256: `397c87e18036b86409f58fa7f80492e5172c8791c8f68c641a1650239d8380d1`. Independently reviewed at model 8.2 and actual-game 8.0 against the generated reference; see `independent-model-review.md`. The combined independent review covers the exact model and fresh native game captures under `.dream-loop/overnight-alien-canopy/runtime-v3/`.

Created locally in Blender by the canopy production agent. The approved generated reference and its provenance/review are in `art/targets/alien-canopy-v1/`. Editable and batch Blender sources, the exact GLB, palette, frozen builder and numeric/render metadata are retained here. Runtime uses the identical file at `assets/models/alien-canopy-v1/model.glb`.

Focused packaged8081 and already-loaded offline replay is retained at `.dream-loop/overnight-canopy-fen-package/`: exact response hash, visible canopy, individual foreground fade with unchanged sibling, native orbit restoration, and current Journal. Zero page errors or failed/external/offline requests; README documents the corrected local blob URL classification in the raw harness receipt. No geometry changed and no cold offline navigation or physical-phone claim is made.

3,788 triangles, two meshes, one material and one embedded 256×256 palette; 256,348 bytes. Dimensions: 3.7m wide ×4.4m high ×2.5m deep, base Y0, glTF Y-up/+Z front. Intact dark warm bark and convex closed paddle crowns deliberately distinguish this scenery from harvestable pale-grain Sapwood.

Only `asset_verge_canopy` is registered. Its collider remains 1.1×3.2×1.1m at offset (0,1.6,0), with unchanged instance placements/scales and explicit false flags. Tall/spread canopy variants and all harvestables are untouched. All triangle surfaces outside the 1.08m low-trunk footprint remain above 2.665539m, or 2.398985m at the smallest existing 0.9 scale. No collision geometry is embedded in this visual.

`builder.py` matches `tools/art/build-alien-canopy.py`. Rebuild into a fresh study directory; the builder refuses to overwrite an existing GLB. `--render-only` imports that exact export for five bounded 512px CPU views. `manifest.json` leaf-contact coordinates describe pre-normalization construction, while its bounds and the separate surface-clearance audit describe the final export.

The V1/V2 superseded studies and the V3 accepted model study are preserved separately in `.dream-loop/overnight-alien-canopy/model-v1/` `model-v2/`, and `model-v3/`. Remaining model limitations are sparse fans and a plain lower trunk. No physical-phone performance claim is made.
