# Rootfall V2 — frozen candidate for independent review

V1 and its independent HOLD7.2 receipt remain intact. This bounded revision addresses the three required model corrections against the unchanged CLOSED V3 target. It has not been self-admitted, copied to shipping, or registered in the world/composer.

The trunk now grows from a fuller left buttress into a lower, narrower right branch system. Major sections vary in height/depth, twist and station shape; repeated material belts and separate flat teal patch meshes were removed. Teal is directly on the bark faces. The working sites use triangular exposed tears and three thin pentagonal skin fragments with diagonal edges, all within Y.95–1.24; no full pale bands or through-cuts. Each timber pair has three continuous fiber turns around both upper shafts before their wood bearing, with a small tucked return.

Five ordinary props share ground origin(0,0,0), unit scale and baked identity transforms. The two retained halves remain the exact same loaded objects in CLOSED and OPEN. Cut planes remain X=-1.65/+1.65; OPEN omits the central interval and both seam instances, and shows the two braces. All retained/brace render vertices and hull points remain outside the required X±1.5 lane. The center meets Y0 and its visible height reaches2.5956m. Existing ridge/return/capsule gameplay still requires native review.

One `rootfall-seam.glb` is reused at(±1.65,0,1), yaw0/scale1, with own ground pivot and the same named `RootfallBark_chunk_0/1/2` meshes. The targeting box is now .74m wide; collision remains recommended false because the main body owns obstruction. Use this candidate's manifest, not V1 dimensions. Braces retain their original feet at(±2.06,.04,2.16)/(±3,.04,1.74), with upper bearing(±2.15,1.48,.9) and lashing center(±2.2564,1.0768,1.194). A few millimetres of foot inset are intentional. Exterior root fingers and bank rocks retain V1's neutral grounding; actual ridge slopes remain parent-owned native review.

Six GLBs total **195,596bytes /2,556 unique triangles /8 unique draws**. CLOSED instances total1,536triangles/9draws; OPEN2,320triangles/4draws. All six pass the existing GLB prop checker. Five world-format convex descriptors pass `validateConvexCollider`, with19/16/16/52/52points. The exact hashes, bounds, contacts and descriptors are in `manifest.json`; `geometry-validation.json` records hashes, common-origin identity transforms and clear-lane checks.

Source consists of six editable Blender files, embedded palette PNG, frozen builder copy and manifest. Rebuild through `tools/art/build-rootfall.py` into fresh directories. The review used Blender4.5, Cycles CPU2threads12samples, after checking about15.7GiB free system RAM. No GPU/TRELLIS, browser or world changes occurred. Fresh exported-model evidence is in `.dream-loop/overnight2-rootfall/model-v2/frames/`: nine full/close/Explorer-scale frames plus a fresh-import review Blend. `validate-candidate.mjs` beside that directory reproduces the geometry contract check. These neutral renders are not native gameplay or physical-phone proof.
