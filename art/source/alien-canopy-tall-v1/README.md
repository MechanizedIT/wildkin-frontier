# Tall alien canopy  -  exact reviewed V2 source package

Stable library ID: `asset_verge_canopy_tall`. Shipping candidate: `assets/models/alien-canopy-tall-v1/model.glb`, SHA256 `2ae3611a270ad6ae424367e46a128492bba391666a274dbd95fa4b744b8d82ba`.

Independent model PASS8.0 and reference PASS8.5 by `/root/level_design_review`. Root authored the reference; `/root/fabricator_model` constructed the model. The source study remains intact under `.dream-loop/overnight-canopy-variants/tall-model-v2`, with V1 HOLD preserved separately. Independent native review now passes8.0 for visible fans, palette, minimum-scale headroom, isolated fade/restoration and Author. Complete form is proved by exact exported-model views plus native Author zoom; normal gameplay crops the upper leader at the existing camera pitch. Final package and owner phone acceptance remain pending. Native proof and review are copied under native/. Original generation.json retains its historical pre-review wording; manifest.json and asset.json carry current admission status. Packaging itself made no runtime changes.

`blend.blend` is the editable 39-part source, with packed palette; `builder.py` reproduces the candidate in a new output directory. `raw.glb` is the exact reviewed export. `generation.json`/`manifest.json` record construction and cost, `normalization.json`/`export-clearance.json` record exact imported triangle-surface/footprint-plane clearance. Contact coordinates in the generation receipt are pre-normalized authoring coordinates; actual saved Blender/GLB geometry owns final positions. `optimization.json` and `asset.json` contain structural checks, not visual acceptance. Front3q/rear are retained as `review-front.png`/`review-rear.png`; front/side/underside are retained under their original names. The source study preserves original render filenames.

Cost:3612 triangles,6860 exported vertices,2 mesh batches,1 embedded256px palette,245472-byte shippingGLB, no rig/animations. Actual bounds3.25m wide - 6.2m high - 2.22m deep,baseY0,+Z forward. Minimum actual surface outside1.08m trunk footprint2.694851m; at minimum authored0.9scale2.425366m. Required2.45m unit/2.2m at0.9scale passes. Static scenery stays weightless/non-harvestable.

Integration contract: preserve all18existing placements/scales and explicit `collision:null`. Do not inherit the standard canopy box. Model descriptor scale1,pivot{x:0,y:0,z:0}; root placements supply their authored scale. Two model batches versus the old recipe's one merged runtime batch is an actual cost increase, not a43-part-to2 runtime saving. Standard/spread/harvestable assets remain separate and unchanged.

Rebuild gently with Blender4.5 background `--threads 4 --python builder.py -- --output-dir <fresh-study-directory>`. Builder enforces fresh export directory and geometry clearance, then renders five512px20sample CPU views. No heavy modifiers, GPU inference or network use. Only the GLB belongs in the game ZIP; source/reference files stay under art.
