# Lantern log manual R4 — source package

This separate repair has been executed once. R3 remains immutable in `../manual-r3/`. Independent review retains R4 as the stronger manual study at **6.3/10**, with art/runtime admission still held.

Frozen inputs are `parameters.json` (SHA-256 `797d79dd9b4d407b9abaf48ba2fd1a88554c6a5d4a101bffc36be87bc5703952`), preserved R3 parameters, and the root-authored repair plan. `build-manual-r4.py` uses the plan's 23 literal angular samples, four bounded inset fissures, lower-belly and broken-rim transforms. It appends the actual saved R3 blend as an unchanged baseline, clones the six stem and cap meshes with structural identity records, refits the three moss patches with bounded actual-log rays, and applies a separate sRGB-to-linear R4 presentation palette.

The [execution receipt](candidate-r4/run-receipt.md) records the actual editable comparison blend, geometry/contact audits and 42 absolute-path renders: seven views at 512/96/48 for both R3 and R4 with a shared bounds fit. R4 has 16 components / 1,390 triangles and 12 exact preserved stem/cap signatures. The blend retains both comparison collections; hide `R3_preserved` when examining `R4_repaired` alone. It is not a single-asset export bundle.

The [independent visual review](visual-review.md) finds improved color and end detail, but the long body still resembles an extruded plank and the far tip a blade. Do not repeat the same five-section/rim-offset method. Preserve this useful partial gain and the original R3 evidence. No GLB, placement, collider, harvesting or runtime change occurred.
