# Grounding hold

The original reviewed input is deliberately rejected by the executable builder because it has no `grounding` object. Root’s identified correction is required: clip the **log only** at a reviewed source-space `undersideClipZ`, then apply an equal and opposite `translateZ` to every completed component. This preserves all literal stem/log and stem/cap contacts while making the final lowest log support a broad planar facet.

No geometry, Blender scene, render, or GLB has been produced. The final input hash and fields must come from root’s corrected probe and focused review.
