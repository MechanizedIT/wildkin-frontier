# Regional crystal blooms — process record

Target author: root, using the built-in image tool (backend image-model version undisclosed). Inputs were the actual portrait baseline and a neutral render of four existing admitted assets. The original generated file is preserved under the Codex generated-images directory; `target.png` is the locked copy. No new model or texture was created for this slice.

Independent target reviewer: `/root/bloom_target_review`, Sol. Target fitness8.4/10 PASS, with a warning that bilateral placement can read like a shrine. Target fitness does not establish implementation quality or physical scale.

Production owners: `/root/regional_place_worker` owns the pure place recipe and ecology/wildlife consumers; `/root/regional_life_audit` owns scenery composition, exclusions and cache lifetimes. Root owns scope, integration, real resource/save lifecycle proof and native/portable captures. `/root/regional_place_audit` independently reviewed cross-system contracts. No worker owns the user's8080 tab.

The baseline is the default seed's computed candidate at(-222.274644,-70.326115), owner chunk(-5,-2), yaw0.957788. Player position is(-216.958160,-66.586464),6.5m from the center along the local front, at a42degree camera pitch and412×915 portrait viewport. It is a disclosed position fixture. The source is generated, not a fixed authored exception. Source and target images use the existing blue-jacket Explorer and HUD.

R1 scored7/10 HOLD. The core's1.18 scale made its actual portrait silhouette about twice the target size, and the broad arrangement clipped its side stones. R2 scales and compresses the formation instead of changing the camera or asset shapes. The physical3.2m target estimate was wrong for this view; selected dimensions follow the actual rendered result. Original target and R1 remain preserved.

The baseline foreground includes ordinary trail stones. Old `seed-22` overlaps the new complete place footprint and must be removed; `seed-19` is outside it. A source-budget coupling originally skipped all ordinary candidates after inserting seven place decorations. R2 restores ordinary sampling before appending the place, preserving safe candidates for the existing capped selection.

Independent contract review prompted four closures before final admission: actual Sunscar weight must be distinguished from starter-reserve fade; nearby complete places need a generation-level spacing bound; missing assets must disable the same place/reservation in every consumer; and ordinary forage clearance must include its scaled footprint. Separate issues found during integration were the Three.js Y-rotation convention and cache overflow returning false absence. Overflow now computes normally without retention, and build release clears the bounded lookup explicitly.

R3 was selected at8.0/10 PASS after R2 at7.6. It preserves R2 scales while compressing the outer layout about20%, moving the core/rear group modestly forward and staggering depths/rotations. Final `receipt.md` separates ordinary input, position/save fixtures, partial/full depletion, portable continuation and physical-phone limits. Source/package/image hashes are in `checkpoint.json`.

The independent workflow audit found this bounded target loop useful, but warned against making its full staffing/three rounds mandatory for every change. `docs/ALPHA_WORKFLOW.md` records the two-batch trial adopted under Chris's latest early-alpha direction. New planning prioritizes useful companions and a staged bounded continent; those future systems are not part of this bloom implementation. The latest user request additionally authorizes a documentation/source backup to GitHub; actual remote confirmation follows the local checkpoint.
