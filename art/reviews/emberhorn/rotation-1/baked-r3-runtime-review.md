# Emberhorn rotation 1 — independent baked R3 runtime review

## Decision: restore runtime retention for the baked R3 recipe

The fresh evidence closes the serialization gap that invalidated the prior final runtime conclusion. `baked-factory-proof.json` compares the actual game `createVisualAssetVisual()` reconstruction with the reviewed R3 source: 32 meshes and 1,302 triangles, exact indices and material flags, with maximum coordinate error `0.00004947m` under the documented four-decimal recipe precision. Other visual assets, asset order, selected metadata, and other top-level world data remain exact. Invalid focused-bake selector inputs reject without altering the world.

The fresh ordinary factory-backed encounter records `ROAM → WARN → CHASE → WINDUP → LUNGE → RECOVER`, then a normal retreat to `RETURN`; no errors or creature/AI pose mutation are recorded. Health stays 5 until the lunge and then changes to 3, remaining there through recovery/retreat. The horned heavy creature and compact contrasting dorsal crest remain readable in the windup, lunge, and recovery frames. The earlier R3 encounter remains historical **old-recipe runtime** evidence, not neutral-kit evidence, and is correctly superseded for admission by this baked sequence.

Recommendation: retain baked Emberhorn R3 for runtime admission, subject only to the root-owned frozen aggregate package gates. This evidence does not claim a new companion, taming, utility, collider, animation, or behavior feature, and no further model pass is authorized.
