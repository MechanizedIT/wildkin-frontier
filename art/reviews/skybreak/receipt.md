# Skybreak plateau checkpoint — September 12, 2026

Selected R3 is usable terrain groundwork: **4.9/10 HOLD**, strongest of three (3.5 → 4.2 → 4.9). Chris delegated selecting the strongest usable candidate after roughly three passes; this is a provisional agent choice, not his personal art acceptance. Linked pad silhouettes, repetitive cliff curtains and sparse vertical habitats remain below the target.

## What the player gets

One bounded plateau recipe north of Camp has **27.69 m approach-to-crown relief**, asymmetric caps, buttresses, open lowland fingers, a supported ascent and separate eastern descent. Four 50 m chunks use 1 m detail; ordinary chunks retain 2 m detail. The public height query matches the actual float32 mesh triangle and neighboring coarse edge. Camera distance/fog/manual pitch and regional cliff shading/shadows expose this larger geography. Current short-face climbing remains bounded to 1.2–4 m; this does not implement arbitrary tall-wall climbing, overhangs, swimming or six regional families.

Forage, scenery, wildlife homes and grass all use regional footprint support. Wildlife's ordinary roaming circle is covered; flee/pursuit navigation is not guaranteed. Existing Camp/terrace and outside-region random identities remain intact.

## Native evidence and fixture limits

The isolated developer save was placed at the approach/crown and health was restored for bounded tests. A dense keyboard-handler route then used ordinary movement; it was not a fresh earned trip from Camp. Overhead/overview images are diagnostic cameras; portrait images are actual gameplay with disclosed view changes.

- Final ascent: 26.37 s, health 5 → 5. Separate east return: 22.17 s, health 5 → 5. See `ascent-final.json` and `return-final.json`.
- Final terrain fall: 30.65 m, health 5 → 1. The follow-up reload exposed a real bug: an incline failed resume overlap validation and fallback restored stale health. This failure was fixed before GitHub publication.
- Corrected replay: 30.64 m fall, health 5 → 1. Literal dev reload retained feet (12.11835, 4.28589, −242.62303), health 1 and grounded IDLE. Exact before/after: `reload-closure.json`.
- Final portable bytes imported the same isolated save through the authoritative import envelope, then literally reloaded. Health 1, the same supported position and all five owned individual IDs survived; resource origins were localhost:8081 only and warning/error log was empty. See `package-reload-closure.json` and `package-reload-portrait.png`.
- Earlier selected-terrain package proof resumed the crown and performed the visible Jump (1.353 m), with health 5 and the same five individuals. See `package-proof.json` / `package-crown-portrait.png`; this preceded the final save-only correction.

The save closure keeps capsule curvature above a legal incline while retaining slope, overlap, proximity and kill checks. Fallback may reuse feet only from the same run/section, then samples live health, XP, bonds, Core, kills and discoveries. Real-Rapier resume tests and persistence regressions cover these sibling contracts.

## Verification and retained evidence

**1,160/1,160 tests**, verify/world/retained campaign/build/validate and ZIP PASS. Package: **44.13 MB unpacked / 20.54 MB ZIP**. `source-hashes.json` records the exact working-file/package SHA256 values; Git may normalize line endings on checkout. No new runtime dependency or network service. Retained campaign checks do not validate a completed frontier economy. Physical-phone performance and touch comfort remain unverified.

Baseline, target and all three overhead/overview rounds and verdicts are retained beside this receipt. The target's left panel remained oblique despite an overhead request; the real overhead and numeric anchors own placement. The inspector is measured sampling, not artwork: its 46 forage / 2 wildlife / 42 scenery items are candidates, not live resident counts. See `inspector.json`, `inspector.svg` and `terrain-measurements.json`.

## Try it

Head north out of Camp, continue beyond the older low rocky terrace, and find the large rising stone caps. Follow the broad left/foreground rise to the highest crown, then return down the separate eastern buttresses. Correct behavior is supported walking with no damage on that route. Sudden sinking, snagging or health loss on the graded route are failures. Stepping off a high edge should hurt; after surviving and reopening, both the lower supported location and reduced health should remain. The current geometry is a prototype awaiting cap/lowland ecology and art improvement.

Next bounded work is one useful ecological outing: lowland supplies, a distinct cap resource/existing-species home and a readable discovery/return clue. Existing abandoned art/composer experiments remain local and outside this checkpoint.
