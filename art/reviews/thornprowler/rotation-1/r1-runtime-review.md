# Thornprowler R1 — independent baked runtime review

## Decision: PASS for existing-resident visual compatibility, with a pose-visibility limit

The fresh proof uses the existing Fungal Hollow resident `f1:w:-58:-26:450` and the baked R1 factory visual. It records ordinary-control progression through ROAM, ALERT, CHASE, WINDUP, LUNGE, RECOVER, and RETURN; the lunge reduces health from 5 to 4 and `errors` is empty.

The alert, lunge, and recovery frames retain the broad grounded limb read and compact thorn ridge in the actual habitat. They show no visible factory/integration regression. The windup frame is materially occluded by the resident mushroom/log composition, so it cannot establish complete leg stance or a fully readable visual telegraph in that framing.

This is sufficient to retain R1 as visually compatible with the existing resident encounter. It does not establish full all-pose limb readability, alter behavior or gameplay, or prove the separate generic built-in rusher-fit sibling; that proof remains a separate requirement.

Root closure: the separate generic fit proof now passes in `generic-rusher-proof.json`; `portable-factory-proof.json` verifies all three reviewed recipes through the actual bundled factory. See the combined Ironspine checkpoint for required aggregate and save proof. The windup occlusion limit above remains.
