# Fungal Hollow closure consistency review

Status: **PASS for local main checkpoint**

No receipt claim requires a source change or blocks the explicit local commit. The receipt keeps source correctness, visual quality, remote fixture evidence, and the restored earned Camp separate.

## Claim-to-evidence check

- **Source PASS / visual HOLD:** consistent. `source-review.md` closes geometry, canonical asset identity, fixed-before-cap admission, missing-kit retry, collision exclusions, deterministic caches, and lifecycle with a PASS. `visual-r2.md` independently retains **6.5/10 HOLD**, specifically because the bank silhouette and floor/bank separation remain weak in the ordinary portrait camera. The receipt does not promote the habitat to visual PASS or ten-habitat completion.
- **Aggregate and package:** exact. `verify.log` ends at **1,400/1,400**, 173 suites, zero failures, `91876.4746 ms`, then records world/campaign/build/validate PASS. `zip.log` records a **20.59 MB** ZIP. The current artifacts independently measure **21,592,110 bytes**, **20.59 MiB ZIP**, and **44.39 MiB unpacked**, matching `checkpoint.json` and the receipt.
- **Raised flower:** supported. `raised-approach.json` and `raised-climb.json` both end `reason: arrived`, remain grounded at health 5, and reach the raised source with ordinary movement. `raised-flower-hit.json` shows source `...:402` changing **3 -> 2** after the field-tool action while carried flowers remain 8; the later physical pickup is what raises the final carried count to 9. This supports one hit and one drop, without implying that the tool action directly banked cargo.
- **Incidental automatic pickup:** disclosed correctly. The earlier safe source `...:400` is already at one remaining before the raised-source proof, while the carried count is already 8. The receipt explicitly excludes the prior Continue-triggered automatic pickup from its manual-input claim and does not count it as a second deliberate harvest.
- **Predator encounter and damage:** supported with a wording caution only. `predator-retreat.json` starts the encounter at health 5 and Thorn health 7, then ends at health 4 with Thorn still health 7 in `RETURN`. The recorded sequence covers ROAM, ALERT, CHASE, WINDUP, LUNGE, RECOVER, and RETURN. Therefore the opening phrase “attack/escape” means **the predator landed one attack and the player escaped**; it is not evidence that the player attacked or damaged the Thornprowler. The detailed receipt paragraph disambiguates this correctly and makes no defeat, reward, bond, capture, or animation-readability claim.
- **Streaming and return:** supported. The northeast legs arrive without further health loss. `stream-out-1.json` reduces resident flowers from four to two; `stream-out-2.json` ends with zero resident flowers and no Thorn. The first return attempt stalls while still outside residency, matching the visible-stump disclosure. `stream-return-detour.json` reaches the outing again, and all eight legs in `return-ramp-and-routes.json` end `reason: arrived`, including the gentler north-ramp approach. Health remains 4 and carried flowers remain 9. Reconstructed flower counts are **1/3/2/3**, matching `final-native.json` and `checkpoint.json`.
- **Identity/no duplication:** adequately bounded. The compact native evidence shows one Thorn before retirement and one after reconstruction/package continuation, with no extra reward or carried-item change. Exact stable origin is primarily source/focused-test evidence; the compact public native JSON exposes count/state rather than the origin string. The receipt's “same single Thorn origin” is therefore a combined source-plus-runtime claim, not a native-telemetry-only claim.
- **Portable continuation and earned Camp:** properly separated. `final-native.json` records `packageExact: true`, one package Thorn, only the local `http://127.0.0.1:8081` origin, and zero browser errors. `package-camp-parity.json` and `developer-camp-parity.json` are both exact. The receipt clearly says the Fungal start, position, route, and AI guidance were developer fixtures and **not** a fresh-player discovery or Camp-earned journey; it separately says the original earned Camp was restored exactly in developer and portable builds.
- **Traversal limitations:** honestly scoped. The native evidence establishes two supported planned routes and a successful detoured return. It also records the visible stump and a steep side channel. The receipt correctly avoids claiming arbitrary straight-line walkability, unaided discovery, physical-phone validation, or sustained performance.

## Commit interpretation

The checkpoint can claim a source-correct, package-valid Fungal outing with useful finite gathering, an avoidable damaging predator, supported planned routes, retirement/reconstruction, and exact restoration of the prior Camp save. It must retain the **6.5/10 visual HOLD** and the disclosed remote-fixture limitations. No push, alpha-release, fresh-player discovery, human-readable attack telegraph, arbitrary-slope traversal, or player-damaged-predator claim is supported by this packet.
