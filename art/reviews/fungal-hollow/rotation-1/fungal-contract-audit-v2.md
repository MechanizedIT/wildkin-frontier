# Fungal route-shape contract audit v2

`FUNGAL_HOLLOW_CONTRACT.md` is authoritative. It requires the frozen route centerlines to retain a 3 m supported half-width on analytic terrain and rendered 2 m triangles. It does not state a zero-height-delta corridor rule.

The route review confirms the intended boundary: a future structural pass may create one continuous lower-side-to-rear bank if it leaves both capsule corridors open and proves support, routes, blossoms, Thorn disk, stones, sampler, and cap parity. This permits a gentle route-height reshape inside existing structural scope; it does not permit narrowing the 3 m width, moving a route/source/home, weakening a stone fit, or skipping `.32 m` physical-footprint and native persistence checks.

V1's zero-delta corridor plus feather was a conservative planning predicate, not an accepted owner or contract requirement. V2 keeps every identity disk hard and treats full 3 m support as the later proof boundary.
