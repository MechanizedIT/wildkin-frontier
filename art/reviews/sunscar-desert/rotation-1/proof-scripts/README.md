Root-owned proof scripts copied from the executed private working folder. Save fixtures remain private/ignored and are not included. The lip preview changes only a temporary render-mesh clone, not source or physics. Historical failed routes and generated targets are separate evidence.

Checkpoint whitespace normalization removed trailing spaces and excess EOF blank lines after execution. The adjacent `whitespace-normalization.json` preserves exact original bytes and both hashes, including the historical Blender scripts and test file. Runtime source and packaged artifacts did not change.
