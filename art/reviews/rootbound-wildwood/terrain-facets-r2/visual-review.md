# Independent R2 terrain preview review

**HOLD — do not integrate the fan.** The repaired preview harness produced valid matching captures: `baseline-valid-r2.png` and `candidate-portrait.png`, with 39 changed vertex copies (32 unique positions) across two chunks and restoration recorded afterward. Frames 723 → 978 → 1,828 are renderer frame numbers, not geometry counts. The original cross-realm typed-array failure is resolved. A `MutationObserver` error remains with no source URL, so its origin is unknown; it does not invalidate the terrain-copy receipt, but should be resolved before reusing the harness.

The candidate's left/right plane split is only faintly readable around the mid-frame flower and open floor. It does not make the Lantern threshold more legible, directional, or attractive at portrait scale. The moving Tidefin differs between the captures and cannot be treated as terrain evidence. The 0.945–1.055 tint variation is effectively imperceptible through the current composition, while the visible change risks a local patch rather than a coherent terrain room.

Retain this as a valid negative renderer-only study. No terrain source, support, physics, resource, route, save, or ecology change is justified. If Rootbound terrain is revisited, use a clearly composed, perceptually consequential landform direction rather than another low-amplitude local floor term.
