# Independent Rootbound terrain-and-coverage R1 candidate review

**HOLD for two narrow formula corrections before the single runtime prototype.** The candidate is otherwise a meaningful advance over the prior plan: it applies a finite band kernel through the actual region/terrain chain, reports 746 changed vertices and 1,849 changed triangles, keeps the sampled source fingerprint exact, and honestly presents camera contribution as modest. Crown terrain is correctly zero under protection and is no longer falsely sold as a raised backdrop.

## Required corrections

1. **Use the stated lane thresholds in the actual kernel and audit.** The approved intent is no displacement within a 3m primary / 2m branch half-width. `candidateShoulder` currently begins its primary feather at **2.5m** (`smooth((primary - 2.5)/3)`), while the lane audit only measures interpolation at `primary <= 2` and `branch <= 1.5`. That can admit a nonzero terrain contribution in the forbidden 2.5–3m primary band. Change the kernel to be exactly zero through `primary <= 3` and `branch <= 2`, and audit those same full limits. Recompute the candidate vertices/slopes afterward.

2. **Bound density to the same full-footprint lane rule and to the claimed class.** `evaluate-density.mjs` presently permits coverage at `shoulder.primary >= 2 + radius` and `branch >= 1.5 + radius`, which is looser than the plan. Its temporary replacement also has no explicit low/non-solid assertion, although the plan claims only already-admitted non-solid lows. Require `spec.kind === 'low'`, exclude solid assets, use `3 + footprintRadius(spec)` / `2 + footprintRadius(spec)` thresholds, and report each admitted record’s band, full-footprint clearance, and out-of-band parity. The current output’s large local increases (for example 62→379 around Lantern/Verge) are a reason to make this boundary explicit before source integration, not a reason to discard the coverage approach.

After those two recomputations, the candidate may proceed as one terrain-and-coverage implementation prototype. Its current slope maximum (0.48345) and modest projected strips are suitable for a prototype, provided the corrected evidence preserves the route contract. Do not claim a finished regional enclosure from these projections; the native comparison should decide that.

## Reviewer correction — current lane contract

My preceding HOLD applied an earlier conservative 6m/4m reading after the current proposal selected the packet-permitted **4m primary / 3m optional lane widths**. That was incorrect. Under the current contract, the kernel's zero-through-2.5m primary and zero-through-2m branch thresholds are conservative, while its `<=2m` / `<=1.5m` interpolation audit covers the authorized half-widths. The existing `rootboundVisibleScenery` function already rejects every non-low and `asset_fen_stone` record immediately before the evaluated pocket return; the evaluator's replacement does not remove that guard.

The candidate's finite terrain chain, source parity, capped .85m relief, and modest camera projections are therefore **GO in principle for one implementation prototype**, contingent only on the promised receipt assertions: every newly admitted record must be demonstrably low and non-Fen-stone, identify an in-band candidate coverage result, clear the current full 2m/1.5m lane half-width by its footprint radius, and preserve all prior selected IDs in each measured window. The plan should remove or mark historical statements that still call 6m/4m and the old 3m/2m values authoritative, so the implementation has one unambiguous contract.
