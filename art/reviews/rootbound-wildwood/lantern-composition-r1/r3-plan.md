# Lantern Grove R3 next action — calibrated upper-left canopy move

Historical narrow proposal, superseded by the whole-region placeholder direction. Do not resume this as an isolated prop loop.

R2 is frozen. The prior R3 world-label proposal was withdrawn: it did not use the portrait horizontal axis and would have moved the broad canopy farther right.

At destination `(-449,675)`, yaw `-2.3`, use the calibrated horizontal camera axis `(-.666,+.746)`. A negative dot from the player is screen-left. The next turn moves only `lantern-margin-left-spread`; `lantern-margin-back-tall` and `lantern-margin-back-spread` remain fixed.

| priority | centre | horizontal dot | `.8m` terrain gradient | role |
|---|---:|---:|---:|---|
| A | `(-437,676)` | `-7.246` | `.142` | preferred clearly screen-left flank, behind and right of the existing foreground log in world space but left in the actual portrait basis |
| B | `(-440,677)` | `-4.502` | `.093` | nearer, still decisively screen-left fallback |
| C | `(-443,679)` | `-1.012` | `.084` | conservative left-edge fallback if A/B fail the existing admission or trunk-collider checks |

The centre gradients pass the current `.32` ground rule. Before any source write, evaluate only these three for the existing full curated admission, 1.1m trunk-bearing support, source/home clearance, and central-corridor collision. Select the first that passes; do not expand the anchor set.

Native capture is the visual gate: the selected spread must produce a clear upper-left foliage edge while the lower-centre travel opening, foreground log, R1 colony, and two fixed rear R2 forms remain readable. If none produces that left framing, hold the kit as insufficient and open one larger asymmetric foliage-edge asset loop instead of adding scatter.
