# Connected Edges R1 — retained and stopped

Baseline: `6f03132`. Chris requested stopping at the next checkpoint; this checkpoint closes the run. No next pass or push.

Fourteen existing root/leaf groups connect Gallery shoulders and outer Grove margins. One structural pass and one focused repair affecting four new IDs are complete. Independent review retains a useful connected shoulder sequence with regional limits. This is a partial placeholder improvement, not a finished habitat or fully enclosed forest room.

All 57 curated records admit; the previous43 and terrain remain exact. Wildlife, source world data, scenery gates and colliders are unchanged. The test-only close-cluster allowance names the east-mid root with the existing east rib; it does not relax runtime admission.

## Evidence hierarchy

`before-*` are the prior checkpoint; `pre-admission-*` record an incomplete initial set. `r1-*` are the complete first pass. `after-*` are the final repaired actual views and authoritative visual evidence. `packaged-interior` matches the final development pose, camera and scenery resident IDs. The overview uses an explicitly diagnostic orthographic camera; portraits retain the production36-degree camera.

`plan-support-clearance.json` is historical first-pass geometry. A stale literal-position audit was caught during repair. `repair-support-clearance.mjs` imports the actual production records; it exposed east-mid clearance of1.518m, so that same repair moved x from-471.5 to-470.9. Final minimum main clearance is2.075m (required2m half-width), optional3.538m (required1.5m). Use this final receipt, not the earlier plan, for retained-source claims.

Final focused test8/8 and integrated verify1,438/1,438 pass. Verify includes the same complete test suite as npm test, which passed earlier in development. ZIP passes at25,940,341bytes. [Computed summary](validation-summary.json) includes package SHA256 and65,730,487unpacked bytes.

Actual short walk:42grounded samples,9.19m/4.003s. Streaming:14new IDs→0→14. Four local portraits plus overview represent all14 additions. These are focused checks for visual-only changes, not a new full3–5minute traversal or performance measurement. No perfect prop grounding or clean-console claim; previously observed MutationObserver error remains unlocated.

## Human inspection

Open the [comparison gallery](review.html). Compare the Gallery entry and interior pairs: the retained version should show opposing shoulder forms and a clearer forward sequence while the player foreground stays open. In the northern and Grove views, the existing tree/mineral anchors should remain recognizable. A continuous wall blocking the route, objects obscuring the central lane, or a vanished anchor would be a visible failure. The broad floor and sparse distant bands are known remaining art limitations.

Execution is stopped. Await fresh owner direction.
