# Connected edges R1 — independent baseline spatial review

The four current poses establish a real continuous-edge need. This is not an asset-quality problem.

- **Entry (-478,620):** the visual mass begins only at the top edge, leaving a long blank upper-middle approach. Add the first Gallery edge group at mid-depth on the side already occupied by the distant shelter/root mass, then alternate the opposite shoulder farther north; do not fill the player’s foreground.
- **Interior (-478,640):** the two large near-frame trunks give a useful local frame, but they do not connect to the sparse upper-center. The plan needs a staggered shoulder chain visible between those trunks and the distant flora, with breaks that retain the main route’s light and forward sightline.
- **North (-478,659):** the existing massive right-side tree is a good terminal anchor. The open left/upper field is the missing counterpart. Continue a lower, irregular left shoulder toward that anchor instead of duplicating another wall on the right.
- **Grove (-449,675):** the deadwood/mineral/log cluster reads as a destination fragment, but it has no larger enclosing transition from the Gallery. Connect toward it with low root/leaf masses placed beyond the open apron and outside the Trailgloam/Mossling disks. Preserve the mineral pair as a local landmark rather than burying it in repeated forms.

The later plan should use roughly three readable depth bands—near existing anchor, mid-distance connecting shoulder, and a farther terminating mass—rather than a uniform border. Keep the central 4 m route and 3 m branch visibly open, retain all 43 curated records and existing residents, and test full transformed group footprints rather than center points. The current baseline is sufficiently sparse that 10–16 noncolliding existing groups can produce a meaningful region-scale gain without new models or terrain work.

## Corrected near-strip receipt — conditional GO

**GO for one visual-only implementation and actual native review.** The available receipt is sufficient for a first pass even though the prose brief is still being written.

The evaluator now measures each transformed full rectangle against the actual route centerlines and applies the authorized thresholds: 2 m for the main lane and 1.5 m for the optional branch. Every proposed hull clears those thresholds; the narrowest main result is 2.922 m and the narrowest optional result is 3.538 m. Full transformed envelopes also remain outside the Trailgloam 3.3 m disk and satisfy the stated forage-center guard.

The Gallery strip is now practical for the four portrait poses: west groups around x -485/-486 and east groups around x -470 to -466 create opposing, staggered shoulders around the outward/return loop. This addresses the actual 3–8 m visual gap rather than placing decoration 20–30 m away. The Grove rear and west-break groups remain separated from the creature apron and keep the mineral/deadwood landmark readable.

This is a bounded visual-low pass, so the nine terrain samples per group and center/radius ecology checks are appropriate planning screens rather than final grounding or traversal proof. Before retention, implementation must preserve all 43 current curated records and all residents, apply each group’s complete transformed footprint, and capture the same four poses. No exhaustive preflight, collision expansion, terrain edit, or new asset is required.

## First native pass — preliminary observation

The initial incomplete `pre-admission-*` captures do not yet support a connected-edge retention claim. Entry remains almost unchanged and blank through its upper-middle; Interior gains only a local frame; North and Grove do not yet show a persuasive continuous shoulder. That is useful evidence about the intended camera problem, not a final verdict on the fourteen-placement pass.

The Entry receipt shows `edge-gallery-east-01` absent from its resident list, and the same snapshot also lacks `edge-grove-rear-02`. The subsequently frozen admission correction moves `edge-gallery-east-01` from z 626.5 to 630, so these captures predate the complete placement set. I will withhold a re-anchor or broader layout recommendation until the corrected Entry and final matching views establish whether the admitted group resolves the visible gap. The corrective review should prove the locally relevant planned IDs resident at each pose, with the planned union represented across the matching captures, while preserving the existing records and the measured route clearances.

## Corrected Entry capture — one focused repair

**HOLD the connected-edge claim for one coordinated Gallery adjustment.** The complete R1 `r1-entry` receipt has the four locally relevant entry and mid groups resident (including `edge-gallery-east-01`), yet the image remains mostly a blank forward floor. The issue is placement relative to the curved route and camera, not missing admission or an inadequate count.

Move only the first four Gallery IDs as one shoulder segment, with no new records, models, colliders, or changes to the remaining ten placements:

- `edge-gallery-west-01`: x `-485.5` to about `-484.6`, retaining z `625`.
- `edge-gallery-east-01`: x `-470.4` to about `-472.5` to `-473`, retaining z `630`.
- `edge-gallery-west-02`: place near the curved outbound shoulder around `(-483,652)`.
- `edge-gallery-east-02`: place near `(-471.5,650)`.

This brings the paired silhouettes into the Entry upper-middle and turns the next pair into a visible continuation for Interior, while retaining the lower player field and central corridor. Recompute transformed full-hull clearance and support for these four changes; the hard threshold remains the approved 2 m main-lane half-width (and 1.5 m optional branch where applicable). Capture the same four views and prove the local groups are resident at their relevant poses plus the intended placement union across the set. Do not reposition the other ten placements unless that single repaired pass still fails to read.

## Final native repair — RETAIN with regional limits

**RETAIN the connected-edge R1 pass.** The final `after-*` captures visibly improve the intended route sequence without closing it. Entry now has an opposing right-hand root/leaf shoulder and a more legible forward boundary while retaining open foreground. Interior reads as a genuine shaded corridor between irregular side masses rather than the previous isolated trunk frame. North carries that composition into the existing large right anchor, and Grove keeps the deadwood/mineral focal cluster readable while adding a modest outer transition.

This is a useful placeholder-scale region gain, not a completed enclosed Gallery. The broad central floor and distant sparse bands remain apparent, especially outside the immediate Entry–Interior run. It should be described as a connected shoulder sequence with a clear route, rather than a finished forest room or a guarantee of physical traversal quality.

The final capture union contains all 14 planned IDs. The final repair uses actual east-mid coordinates `(-470.9,650)`, whose production-record receipt measures 2.075 m minimum main-lane clearance and 3.538 m minimum optional-lane clearance; the only declared close pair is the east-mid root with the existing Gallery east rib. No collider, terrain, wildlife, or new asset was added. The reported 1,438-test, verify, and ZIP passes support implementation consistency; the visual verdict is based on the four matching native frames.

No further repair is recommended at this checkpoint.
