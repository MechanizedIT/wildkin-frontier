# Rootbound evidence index

Canonical game evidence is `checkpoint-{arrival,gallery,destination,overhead}.png` and `checkpoint-captures.json`, followed by `final-journey.json`, `portable-proof.json` and `validation.json`. The final source retains restarted R2 after the rejected R3 feasibility experiment. The original bounded game batches are complete; the explicitly authorized tree workflow study has its own source and reviews under `../../source/rootbound-buttress-study/`.

## Comparison and history

- `restart-baseline-*`: immutable f455dcb comparison frames, including the destination pose amended before the restart.
- `restart-blockout-*`: restarted structural pass R1, held at 3.1/10.
- `restart-r2-*`: restarted repair R2, held at 3.6/10.
- `restart-r3-feasibility.md`: final structural method rejected on traversal/support feasibility; R2 retained.
- `checkpoint-*`: final matching frames after the retained lantern bank was confirmed restored.
- `final-arrival.png`, `final-gallery.png`, `final-destination.png`, `final-overhead.png` and `final-captures.json`: earlier closeout captures made before that confirmation. They are superseded by `checkpoint-*`; their filename is not evidence of final status.
- `final-walk-*`, `final-useful-interaction.png`, `final-reload.png` and `final-journey.json`: final ordinary-input journey and literal reload, distinct from the superseded fixed-view `final-*` captures above.
- `baseline-*`, `r1-*`, `r2-*`, `independent-r1.md`, `independent-r2.md` and `superseded-r2-*`: historical evidence from the owner-superseded first habitat attempt.
- `exploratory-*`: intermediate mixed-source diagnostics. They are preserved but do not establish final play or visual acceptance.

Generated direction lives under `../../targets/rootbound-wildwood/concepts/`. It must never be presented as implementation evidence. Actual browser screenshots use an isolated copied save; complete private saves remain ignored. The overhead is only a loaded-neighborhood diagnostic with fog disabled. Phone-size browser viewports and guided coordinate-assisted walking do not prove physical-phone performance or unaided human navigation.
