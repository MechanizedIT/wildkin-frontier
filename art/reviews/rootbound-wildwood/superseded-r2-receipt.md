# Original Rootbound attempt — superseded by fresh owner restart

September 14, 2026. The original structural pass and one repair reached independent **3.9/10 HOLD** and **3.6/10 HOLD** respectively. The owner then supplied four visual concepts, requested separate target/director roles, and explicitly authorized restarting this habitat pass using the new selected target. These records are historical evidence, not final acceptance of the restarted source.

The original baseline derives from Git `f455dcb`. `baseline-*`, `r1-*`, and `r2-*` are matching actual game captures. `superseded-r2-source.json` records the exact repaired source hashes; a local source copy is retained in the ignored proof workspace. Earlier exploratory images used changing intermediate source and are excluded from final proof.

The frozen original R2 completed a 279.586-second guided ordinary-keyboard circuit, seven reached legs, no jump/detour fallback, health 5/5 and no page errors. Only the starting position was set diagnostically in an isolated browser seeded from a copied earned Camp save. This proves directed traversal, not unaided discovery or physical-phone play.

Holding the ordinary field tool at the existing berry plant collected four berries (pack total 17 → 21) and depleted `f1:r:-9:13:3`. The final literal reload preserved inventory, ecology and every progress field except `activeRun.feet`; the latter resettled 0.905m. Full-save equality therefore did **not** pass. The earlier 39-second baseline probe also changed feet on reload, but moved before its export and changed atlas/resource records, so it is not a matched persistence comparison.

The first 40,000 renderer intervals sampled 4.9ms median, 6.9ms p95 and 162.3ms maximum. The cap ended before the full route: these are bounded headless Edge laptop samples, not full-route worst-case timing or a measured performance improvement. Physical-phone performance remains untested.

All 26 original discovery records, including the earned grove outside this bounded area, and selected protected terrain samples matched. Focused Rootbound/source checks passed 18/18 and broader terrain/scenery/ecology/wildlife checks 110/110. The root's aggregate `npm test` passed **1,416 tests / 173 suites**, zero failures, in 97 seconds. The subsequent `verify` was deliberately interrupted after the owner restarted the pass; ZIP and portable checks were not completed for this superseded source. No integrated-checkpoint or package PASS is claimed here. A reviewer also accidentally expanded an earlier attempted focused npm command into a full-suite run; that was not an integration gate.

The restarted pass uses [the selected target and brief](../../targets/rootbound-wildwood/concepts/visual-director-review.md) before implementation. It retains the valid source/support contracts and remains inside Batch A, with one fresh structural pass and one repair. No push occurred.
