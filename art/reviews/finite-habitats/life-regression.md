# Finite habitat life regression

## Scope

Read the ecology, wildlife, and scenery test families against the frozen finite habitat allocation. Production sources were not changed. The review distinguished geographic witnesses that moved out of their former terrain grammar from behavior regressions.

## Initial result

Command:

```text
node --test tests/frontierEcology.test.js tests/frontierEcologyRuntime.test.js tests/frontierEcologyState.test.js tests/frontierWildlife.test.js tests/frontierScenery.test.js tests/frontierSceneryRuntime.test.js tests/frontierSceneryVisual.test.js
```

Result: **84/92 passed; 8 failed**. All eight failures were stale coordinate or whole-selection expectations after the finite allocation changed the owning habitat, dry support, or unprotected peripheral recipes. No production defect was found.

## Focused corrections

- Moved the Sunscar bloom fan witness to chunk `(-34,-4)` for forage/runtime and to `(-35,4)` for the scenery overlap proof. The tests still require the admitted crystal, fixed finite IDs, collision/yield, whole seven-prop formation, ordinary-recipe exclusion, neighboring-owner clearance, caps, and failure fallback.
- Moved the two-crystal cleft witness to `(-37,-10)` and the pure Sunscar scenery/wildlife witness to `(-44,-8)`. The tests now explicitly confirm `sunscar-desert` ownership before asserting the mineral-heavy, treeless, sparse ground-cover and Emberhorn signature contracts.
- Updated the `(6,2)` coastal contour to the three currently dry supported seeds. The test retains both shoreline clusters, a central exit, full-footprint land support, asset admission fallback, deterministic residency selection, and near/global caps.
- Kept the starter selection hash exact. For Skybreak, kept all 19 staged identities exact and separately bounded the legitimately geography-sensitive peripheral selection; dense infill remains excluded.

## Final proof

The same seven focused files passed **92/92** in **9.18 s**. This includes ecology state/runtime lifecycle, wildlife caps and return bounds, scenery recipe/cache/preload/runtime/visual lifecycle, ground-cover exclusions, stable IDs, terrain support, and fixed-site clearance.

No aggregate, build, browser run, or production edit was performed.
