# Project skill recovery snapshots

`wildkin-asset-forge/` is the September 11, 2026 snapshot of the project-specific skill installed at `C:/Users/cwood/.codex/skills/wildkin-asset-forge`. It includes the skill entry point, agent metadata and referenced workflow notes. It is deliberately stored outside active skill-discovery directories to avoid competing duplicate installations on the current PC.

On a replacement machine, read `wildkin-asset-forge/SKILL.md` directly or install this directory under that user's Codex skills directory if no copy is installed. Compare an existing installation before replacing it. Configure local tool paths afresh; the snapshot contains workflow instructions, not Blender, model weights, plugins or credentials.

The active repository workflow remains `.agents/skills/wildkin-development/SKILL.md`. Current scope and asset admission status remain in `docs/CURRENT_SLICE.md` and the asset documents, not in this dated recovery snapshot. Update this backup deliberately when the installed project skill changes materially. Snapshot hashes are retained in `art/source/tidefin-candidate-v3/backup-manifest.json`.
