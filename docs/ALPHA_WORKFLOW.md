# Wildkin playable-batch loop — two-batch trial

September 13, 2026. Chris requested an independent comparison of Dream Loop, his earlier Rapid Alpha Producer skill and this project's actual results. This is a producer trial for the next two playable batches; shared skills used by other projects remain unchanged.

## Evidence to keep

- Explicit owners and separate review questions found real generation, clearance, asset and persistence problems in [regional blooms](../art/reviews/regional-blooms/process.md).
- An attainable locked target and independent judgment improved that bounded composition from 7.0→7.6→8.0. Keep this when appearance is the main uncertainty.
- Profiling before architecture changes produced measurable [streaming improvements](../art/reviews/streaming-preparation/receipt.md). Correctness work starts with a failing behavior; performance work starts with measurements.
- Disclosed fixtures, ordinary input and literal reload separate tested mechanics from earned outings. Keep those distinctions.

Rapid Alpha Producer offers useful batching and evidence-reuse ideas; its effectiveness in the earlier project is unverified here. Dream Loop's threshold is also insufficient by itself: [province terrain](../art/reviews/regional-provinces/receipt.md) stayed held after three rounds. Repeated structural gaps require a different approach or deferred polish, rather than more small color/scale changes.

## The loop

1. **Choose one playable outcome.** Group one to three related slices around visible player success/failure. Name owners, uncertainty and the exit in CURRENT_SLICE; avoid a status document per subtask.
2. **Establish the right baseline.** Use an actual short journey for interaction, a failing contract for correctness, timing for performance, or normal-camera images for appearance. Reuse feasible locked targets and unaffected evidence.
3. **Build and integrate early.** Root owns shared composition, canonical data, save schema and browser state. Workers get disjoint file chains; overlap independent recipe, visual or research work only when useful. Prove a representative effect before multiplying content.
4. **Review the principal risk.** One independent reviewer returns one consolidated repair packet. Use a visual judge for appearance, a specialist for save/physics/determinism, and both only when both risks materially changed. Root does not repeat a completed specialist investigation.
5. **Repair and reassess.** Rerun only invalidated proof. Default visual work gets one correction pass; a second needs a structural change. Reserve three rounds for a hero surface or major legibility risk. If the same gap repeats twice, stop that method, retain honest HOLD/debt and continue with a usable candidate under owner delegation.
6. **Play and checkpoint.** Complete one ordinary-input story per integrated batch. Freeze source and run the required aggregate/verify/ZIP once; retain focused failed-save and real Rapier proof where relevant. Check portable/offline behavior for the alpha candidate and changed loading/build surfaces. Update changed documentation and commit a cohesive batch on main. Push only under separate authorization.

## Dream Loop's job

Use it for a new hero place/creature, major terrain silhouette, camera/UI composition or failed normal-play readability. Build the target from the actual camera and admitted kit; check physical feasibility before production. Ask the judge for the three consequential gaps and an honest score. Actual gameplay owns scale/readability conclusions when a generated target is misleading. Ordinary save fixes, pure helpers, text and cache optimizations need no image target.

## Avoid observed waste

- Preserve valid journey segments when a harness endpoint, key timing or tool delay invalidates another segment. Repair the harness and repeat only affected proof; keep one complete fresh-player outing for the alpha candidate.
- Do not duplicate review questions or fill every worker slot. Parallel production needs nonoverlapping owners.
- Do not repeat aggregate checks after docs-only edits or every visual round. Group work into playable checkpoints without weakening repository requirements.
- Keep one compact receipt and actual evidence. Link old history instead of copying it into every restart document. Report player outcomes and limitations.

## Measure before claiming improvement

For each of the next two batches, record start/end time, completed player outcome, worker/reviewer roles, visual rounds, native journeys, aggregate runs and work repeated because of tooling or coordination. No new telemetry framework is needed. Compare useful outcomes and rework.

The review suggests repeated gates can add cost, but does not prove they dominate time; the latest aggregate took about 65 seconds. Browser/tool round trips, documentation and review handoffs also need observation. Retain or revise this trial after two batches. Never improve a speed metric by weakening save ownership, terrain support, deterministic caps or honest mobile/offline claims.

## Trial1 result — first useful outing

Gameplay/checks ran07:16–08:22UTC; documentation/commit closure followed. Two disjoint production workers, one independent reviewer and root integration delivered the derived purpose, readable portrait card and a native-discovered stale inventory-HUD fix. A worker reused its idle slot for affected documentation. One consolidated review repair; no generated target/redesign rounds for this behavior-first change. All1,243 tests and package checks passed in one aggregate; the test phase took65.1seconds. [Actual story, screenshots and limits](../art/reviews/alpha-outing/receipt.md).

The expensive part was the first earned journey and tool handling: source inspection interrupted a timed tame; an input helper initially used click for a pointer-only control and missed the switch to contextual Bond; straight routes needed rerouting around real obstacles. Native play exposed the next genuine release gap—portrait construction framing. Keep the successful earned Camp/save for batch2 and test only changed construction/framing paths. Separate timed input execution from analysis, use the actual currently visible control, and finish one input owner before starting another. This batch does not yet establish that the trial is faster overall; assess after the second batch.

## Trial2 result — portrait construction

Trial2 gameplay and its integrated gate ran08:29–08:44UTC, about15minutes; documentation closure followed. The feasible actual-camera target was locked around08:34 and gameplay/source proof reached its main result around08:41. Two Sol production workers and one Sol reviewer covered disjoint placement and camera/UI owners; root integrated and ran native play. The independent review scored8.5/10 PASS, so no repair round followed. All1,252 tests passed in65.684seconds, the integrated verify/package gate passed, and developer/package reload matched the affected saved state exactly. This behavior/camera batch adapted the visual workflow by locking an attainable target from actual geometry and the existing camera rather than generating new art.

The earned/refunded Camp fixture avoided repeating forage and bonding. Normal portrait UI placed and immediately used both bed and garden without landscape, position or camera overrides; cancellation and external lifecycle restoration were checked separately. Screenshot emulation caused three invalid captures and one ineffective click. Those capture/input steps were repaired without repeating the valid journey. Trial1 took about66minutes through gameplay and its main gate versus about15minutes for Trial2; the unequal scopes do not prove a speed improvement.

Keep this scoped hybrid loop: choose one player-visible outcome, reuse valid state/evidence, split only disjoint owners, lock an actual feasible target, request one focused review and run one integrated gate. This two-batch observation supports that project workflow provisionally; it is not scientific validation or a global claim about either source skill.

## Subsequent batch lesson — native mining

Cragbreaker reused an admitted mineral pocket and existing ability presentation, with two disjoint workers and one save/physics review. The review found one overwritten failure message; one repair closed it. One aggregate covered1,260 tests in66.839seconds. Keep behavior baselines for this kind of change; no new hero-image target was needed.

Native tooling still caused avoidable delay. Always dismiss the Continue screen and check blocking state after literal reload before issuing timed movement. Use a short input helper with an explicit cancellation/finally cleanup; a helper that remembers held keys while a modal discards them can stall after dismissal. Restore screenshot emulation before input. Follow current pickup positions during short actions because retained visuals may be recreated after30seconds; do not chase stale coordinates. Keep these harness mistakes separate from valid gameplay and repeat only affected steps. The same-session forced residency check is an explicit fixture, not an earned traversal.


## Tidefin result — timed status and a real encounter

The same two disjoint Sol workers and one independent reviewer delivered a combat-owned Ward timer and existing HUD/dome presentation. Native comparison exposed a real territorial warning race; the UI worker took the now-disjoint creature fix after its original work finished. One integrated gate passed1,270 tests in66.775s and the complete protected approach→ore collection→physical Camp banking→developer/package reload closed with independent PASS. No new generated art was needed for this small actual-DOM status target, and shared skills remain unchanged.

An apparently frozen actor can be resetting rapidly: inspect actual state writes and target eligibility before blaming pause or streaming. Here aiTimer and warnTime differed by one fixed tick. A temporary bounded diagnostic was removed after proving the cause; both rusher and spitter siblings were fixed. Settle screenshot metrics before activating a three-second status; resizing within that window invalidates the visual sequence. A browser observation timeout may leave the input helper running, so recover its same completion handle before retrying. Preserve valid gameplay, disclose automated WINDUP timing and never claim a missing impact image. Exact final save parity and expired/native versus active/unit reset evidence are separate claims. [Receipt](../art/reviews/tidal-ward/receipt.md).

## Coast result — integration before further art

Three disjoint production owners delivered continent/land admission, player water movement, and ocean/atlas presentation; root integrated UI, save/resume and native play. One independent reviewer consolidated shelf, direction and WADE input issues. Native play then exposed a timestep-dependent current: composing one desired velocity before acceleration fixed it without more state. Source/native review passed. Three visual rounds reached5.5/6.1/6.4 and remain held. Increasing prop size and moving a witness nearer the sea did not create the target headland or put enough dressing in the actual camera. Next visual work must first prove projected landform/asset placement in the ordinary view; do not spend another round on off-camera scaling.

The first aggregate exposed one old high-terrain test location now under the ocean. Move such a witness to actual inland terrain without weakening its height/color assertions. A second full integrated gate passed1,300 tests in71.858s. Developer and package canonical reload parity, packaged9.908s shore entry and local-only resource origins were verified. Keep the scoped hybrid workflow and unchanged shared skills. Tool overhead included an incorrect camera debug accessor and torn full-diagram captures; the final correct render used a viewport that fit the host surface. Bounded input completion handles preserved valid journeys. The diagnostic continent chart is not the player atlas. [Receipt](../art/reviews/frontier-coast/receipt.md).

## Grove result — reuse the kit, measure the frame

Three disjoint Sol production owners covered place/catalog, seals/lids and resident lifecycle. Root owned canonical reward data, target/native play and integration; independent reviewers covered the changed save/residency contract and visuals. The actual baseline was captured11:34:27UTC; the single integrated gate finished11:54:52, with final R3 capture11:55:49 and portable/documentation closure afterward. This roughly20-minute baseline-to-gate interval excludes earlier audits/planning and later closure; it is not a comparable whole-task benchmark. All1,319 tests passed in73.884s. One family-admission repair packet plus a narrow scaled-collider review closed source risk; native play proved full-health Bloom, lid delay, collection, two-site physics/residency and complete final save parity. No new asset generation was needed beyond the proposed scene target.

Visual passes5.2→7.4→7.7 remain HOLD. The useful correction was moving actual objects into the portrait frame, not adding detail. A real projection check put the arch below the objective panel; leaf occlusion still weakens it. Next target work should include the actual kit silhouettes as well as the actual camera so a generated small chest or complete tree crown cannot set an unrealistic reference. Stop at the delegated third pass and keep the strongest usable candidate.

Avoid reading compact world JSON with a line-oriented search: one match can dump the entire model dataset. Parse the requested fields instead. Do not print image-generation data URLs; display the image and retain its path. Wait for an owned dependency to be written before running its integration test. When field XP is under test, create an active run rather than expecting it to become banked XP. A residency shift must cross the actual5×5 boundary to test retirement; correct only that diagnostic endpoint. Keep legitimate new atlas-reveal bits separate from loss/duplication: record them, then establish exact canonical parity from the stable selected save.

## Earned second outing — let connected play choose the work

Two disjoint Sol writers fixed the derived field purpose and Camp furniture/yard selection; one independent reviewer covered both small contracts. Root continued the earned one-Mossling save through preparation, a guided physical grove journey, collection, banking and garden use. The first recorded Plant was12:26:27UTC and the integrated gate finished12:55:38; audits began earlier and portable/documentation closure followed. Thirty bounded movement legs accounted for361.392seconds of input, including fence/tree attempts; inspection pauses and UI actions are separate. One aggregate passed1,322 tests in76.024s. No visual redesign or generated target was needed for these behavior/copy fixes. [Evidence](../art/reviews/alpha-preview/receipt.md).

The route audit omitted a closed Camp fence; ordinary play found the open exit. On return, sideways motion against a tree fooled a displacement-only stall check until the deadline. Future bounded input helpers should measure progress toward their waypoint, release held keys on completion and preserve valid earlier legs. Keep terrain-query guidance distinct from unaided discovery and real collision proof. Capture only after the changed UI has rendered; an immediate screenshot can precede the already-updated DOM. Two invalid standalone-SVG captures were discarded and the vector chart retained; game-canvas capture metrics do not transfer reliably to an SVG document. Exported saves are envelopes with a `progress` member, not the live state object. These harness corrections required no game-state grants, physics stepping or repeat of the earned capture.

The next worthwhile visual task comes from the actual journey: fill sparse ecotone views using admitted grass/scenery and existing budgets. Keep shared/global skills unchanged. This scoped hybrid remains provisional; useful output and honest proof matter more than counting loops.

## Plant readability — reject a target mismatch before production

Fresh Lush baseline13:23:46UTC, generated target13:31:13UTC; one scale pass, one principal reviewer and one aggregate passed1,323 tests in74.922s. One Sol production worker owned the runtime/tests; a read-only scenery specialist projected an in-memory triad and fairer chunk selection. Both still put zero scenery anchors in the actual sparse frames, so neither was implemented. Root narrowed the target honestly to readable plants and preserved the wider density problem. Native W ran2.011s/4.221m to the saved-open grove after an explicit fixture; exact earned-save restoration passed developer/package reload and Continue. Final source blended the gain through influence to avoid a new starter-edge size step.

Planning took longer than this small production change. Next density work should begin with one measured actual-camera experiment and explicit budget, not repeated algorithms constrained by unproven old caps. A generated scale-only target cannot demonstrate scene fullness. Reuse this evidence. Browser calls timed out during reload/emulation and one awaited frame-sample promise; a recoverable120-frame record and ready-state-separated input retained valid proof. Reload can reset viewport metrics. The save canonicalizer returns an object: compare serialized values, not object identity. Those diagnostic failures changed no game state. Shared Dream Loop/Rapid Alpha skills remain unchanged; the local hybrid is still provisional.

## Density batch lesson

Low-count shuffles and144/576-prop diagnostics failed actual-frame coverage. A final64–256-per-chunk experiment, measured geometry reuse and one locked target produced a materially fuller native view; the independent judge still rated it6.8HOLD. Keep that honest result and stop seed/scale tuning when patch composition is the remaining gap. Native movement exposed0.29–0.42s boundary frames despite normal33ms travel; steady rendering alone cannot prove streaming. Profiling places most visual-build time in ground footprint checks, so the next step is retained/progressive preparation rather than another small height-only tweak.

One independent source repair packet found an inherited ground-instance buffer leak and a new infill/fixed-site exclusion gap. Close sibling metadata consumers when moving a canonical record. Two aggregate invocations were required because root's new cache assertion preceded the protected-selection correction; the second passed all1,337 tests. Wait for owner handoff or rerun affected focused integration proof before freezing. No shared/global skill was changed; this applies the project hybrid workflow provisionally.

## Scenery preload lesson — reprofile after the win

Two disjoint workers owned resumable recipe and visual/cache work; root owned integration/native proof, and one independent reviewer covered the shared ownership boundary and documentation. One aggregate passed1,350 tests in77.391s. Reusing the prior6.8HOLD appearance avoided an unrelated visual round. Native boundary B reduced scenery270.5→16.6ms and the maximum frame420.6→161ms, with one prepared publication and no preparation failure. The measured preparation maximum was15.5ms, so a3ms loop budget is a soft checkpoint between indivisible work units rather than a hard frame guarantee. After removing the dominant phase, measure the whole boundary again: ecology now accounts for86.6ms and terrain preparation20–27ms. Scope the next owner from those phase timings instead of extending the scheduler speculatively. Evidence: `art/reviews/scenery-preload/receipt.md`.

## Continuing alpha passes — owner steering September13

Chris explicitly asked to keep replanning after each completed plan: revisit core loops and mechanics, improve natural presentation and UI/UX, and consider one useful secondary feature per pass. Start each pass from current play and the release gaps; an optional feature is considered, not a quota that displaces a broken core loop. Use a polished, feasible continent image as the next geography target, compare the actual sampled outline against it, and retain the existing three-round ceiling. The latest large/non-round continent request takes immediate planning priority; measured ecology loading remains queued debt.


## Larger continent lesson

Use a generated map as a frozen macro reference, then compare the real sampler at the same extent; separate silhouette admission from vegetation and gameplay admission. Three structural rounds were sufficient (6.5/7.8/8.3). Inspect finite catalogs and atlas capacity before multiplying land area. Eager startup scans and persistence caps can scale independently of live residents. Preserve meaningful support/selection tests when geography intentionally changes; do not blindly update hashes. Native fixtures prove new coast controls while the original earned Camp remains a separate exact-save witness. Pre-alpha region production may regenerate unvisited geography rather than accumulating compatibility layers.


## Finite habitats — preserve meaning when geography changes

Fixed ten-site ownership replaced the repeating field. Independent topology sampling caught small coast-clipped components; two25m site adjustments removed sampled islands without a polygon framework. Retargeted tests now exercise actual Sunscar/ecotone/bloom locations while keeping support, clearance and lifecycle assertions. A real grass/forage overlap was fixed; an all-scenery filter added unnecessary generation work and was rejected for a bounded forage-only build check. Source and focused proof converged before one aggregate:1,361tests/84.251s and package gates. Native fixtures preserved the earned save and crossed Heartwood→Rootbound. Do not use timings recorded during another build reload as controlled performance comparisons.

A120m camera far plane does not make90m-ahead terrain visible in a42degree downward portrait view. Project target anchors before art: the approximate top ray meets level ground18m ahead of the player. The Caldera target must emphasize near breach shoulders and5–20m content, leaving the full ring to movement/orbit/overview. Render authored SVG reports with bundled sharp when browser captures tear; no runtime dependency is added. Keep the shared skills unchanged.


## Caldera lesson — screen space and one input edge

Caldera used three art rounds and ended at1.5/4.0/3.8 HOLD despite source support passing. A5–20m depth assertion and projected center points did not account for complete scaled geometry or the actual inventory/objective HUD. Require a rough native blockout with full transformed bounds and real HUD safe areas before investing in a new composed-place target. Do not reward offscreen geometry in the visual review, or erase density merely to satisfy an arbitrary empty-lane choice. The next separately scoped camera/HUD pass must address this root cause before another content rollout.

A quick F tap exposed a shared catch-up defect: the same edge reached several physics substeps and queued a second swing. One first-substep gate fixed it without another input abstraction. Native injected80ms frame delay proved [true,false,false,false] and one resource decrement; an intentional hold still produced two. This is stronger than another test duplicating the latch implementation. Pause through an ordinary UI before inspecting code near hostile wildlife; an unattended R2 character died while root investigated, whereas the bounded R3 warning/retreat succeeded. Preserve both the failed attempt and the valid final evidence.


## Portrait-view lesson — prove the frame before the art

Three native camera blockouts isolated a shared source of apparent sparsity: clipped complete props and permanent inventory rows. The selected36°/11.591m view was checked at Camp, Caldera and an earned grove before its generated reference and production contract. Two disjoint workers changed four runtime/style files, cross-reviewed each other, and one presentation round passed8.8. Keep terrain-art holds separate from this usability pass. Actual pickup/expiry and retained construction distance mattered more than a larger screenshot score. Wider visibility raised grove draws67→102; record that cost and avoid inferring phone performance from a smooth180-frame Caldera witness. One aggregate passed1,379 tests/86.428s. No shared/global workflow skill changed.
