# Fen observatory receiver — September 11 candidate

A distinct alien receiver replaces the repeated arch at Shatterfen's existing observatory. Its supported bowl, rim, spokes and core move together around the two side bearings. The fixed plinth and pylons remain still. It starts tracking when the existing Fen waypoint is discovered; the presentation grants no rewards and does not change travel or extraction ownership.

The target was generated from the actual region screenshot and Chris's approved Explorer style sheet. Independent reference review rejected a first image whose third central support would prevent the bowl tilting. The corrected two-bearing target passes 8.4. A separate model implementer and judge admit the exact GLB at 8.1 after inspecting neutral, both tilt limits, rear and underside. Independent actual-game review passes 8.0: side views clearly distinguish the changing bowl inclination from its stationary supports. Plain stone surfaces, a dark bowl and applied-looking inlays remain fidelity limits. Sampled poses do not establish continuous animation smoothness or phone performance.

Exact model SHA256: `91b2e30d4813ee8b67f3a122f84e6641bdf99b4eead74e9c84835ddf0a32ccee`. It uses 1,616 triangles, three rigid mesh batches, one 256px palette and 111,120 bytes. The `ReceiverTiltPivot` node has an identity rest rotation, local horizontal X axis and pivot at glTF (0, 1.98, 0). The neutral 18-degree bowl tilt is baked into its geometry. Only the coaxial shafts live in its `ReceiverAxles` child; they have no independent motion. Continuous inspection of every non-axle vertex over ±10 degrees establishes 67.4mm lateral and 209.5mm vertical clearance from fixed support geometry. Runtime uses a narrower ±8-degree, ten-second cycle.

The existing prop ID now sits behind the unchanged waypoint, at (23, .66, 4.8), with zero yaw and unit scale. Its solid 3.6 × 3.16 × 2.4m footprint is deliberately non-traversable. The foundation sits slightly into the supported bank; the approach to waypoint (23, .7, 8) stays clear. Canonical comparison verifies only one new asset descriptor and that existing prop changed; all other world data is identical. Pause freezes motion, reduced motion holds neutral, and Author/reset/inactive regions restore neutral through the existing presentation owner and single game loop.

Native 844×390 review verifies waypoint discovery starts tracking, Pack freezes and resumes it, reduced motion returns neutral, the solid front stops movement, and the neighboring waypoint still returns to Camp. Author's native Focus/zoom and isolated Edit show the complete neutral model; a separately placed 0.8-scale copy preserves model, neutral pivot and scaled collision after reload. Initial wide Author screenshots failed to frame the model and remain excluded; later frames 16/17 close that gap. Zero page errors were recorded. Disclosed start/restart fixtures precede native inputs; this is not an earned campaign run.

Sources, reviewed target and exact export evidence are retained in `art/source/fen-observatory-v1/` and `art/targets/fen-observatory-v1/`. The model passed bounded four-thread CPU rendering; no local inference or expensive modifier was needed. Runtime proof and independent judgment are in `.dream-loop/overnight-fen-observatory/runtime/`. Packaged exact HTTP model hashing, native discovery/tilt/Pack and the current Journal pass online and again already-loaded offline. The combined canopy/Fen proof has zero external requests or runtime errors; its corrected local-blob classification and raw receipt are explicit in `.dream-loop/overnight-canopy-fen-package/`. Whole-region bank/path composition and physical-phone performance remain separate work.
