# Standard alien canopy — September 11 candidate

The standard decorative canopy now has a Blender model with folded paddle leaves, unequal connected forks and warm faceted bark. It replaces nineteen existing Camp, Verdant and Shatterfen instances through the same asset ID. The separate tall and spreading variants remain unchanged; harvestable Sapwood retains its visibly different removable ribs and stump.

The original target combines the actual game canopy with Chris's approved Explorer style reference. Two prior implementations were held for platter-like or crowded rounded crowns and dark, unreadable forks. V3 keeps the target and corrects its leaf geometry, branch gaps and local palette. Independent review passes the exact final model at8.2/10 and actual gameplay appearance at8.0/10. Sparse fan overlap and the simple lower trunk remain fidelity limits, not claims that all flora or level composition is finished.

Exact GLB SHA256: `397c87e18036b86409f58fa7f80492e5172c8791c8f68c641a1650239d8380d1`. It has3,788 triangles, two batches, one256px palette and256,348 bytes. Editable Blender, deterministic builder, target, five exported-model views and review receipts are retained in `art/source/alien-canopy-v1/` and `art/targets/alien-canopy-v1/`. The folder's v1 is a stable asset package name; the admitted bytes are the V3 revision above.

The existing1.1×3.2×1.1 collider, nineteen placement transforms and fourteen solid/five non-solid flags remain unchanged. Actual exported surface outside that footprint starts at2.665539m, or2.398985m at the smallest0.9 scale. Native844×390 movement stops at the trunk and passes beneath the crown. Foreground orbit fades both target meshes to0.25 while a neighboring copy stays opaque; the opposite view restores both meshes. No game lighting or camera setting was changed to earn the score, and no browser exception occurred.

Evidence is in `.dream-loop/overnight-alien-canopy/model-v3/` and `runtime-v3/independent-review.md`. Earlier V2 Author placement/reload proof covers the unchanged descriptor, hierarchy and footprint; V3's exact palette/geometry was inspected in Play. Packaged V3 now passes exact HTTP model hashing, visible native movement/orbit, individual foreground fade and restoration, repeated after switching the already-loaded browser offline. The combined canopy/Fen/Journal proof records no external requests or runtime errors; local texture blob URLs were corrected in the harness's origin classification, with the raw receipt retained. See `.dream-loop/overnight-canopy-fen-package/`. Physical-phone performance remains pending. This is one admitted asset family, not whole-world acceptance.
