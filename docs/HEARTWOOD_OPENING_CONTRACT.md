# Heartwood opening — completed bounded contract

### Production contract: the first Heartwood clearing

The actual Camp walk found an empty north field before the starter encounters. Two temporary blockouts established four near canopy trees and sixteen grouped low plants around the ordinary portrait view at approximately `(0,-32.86)`. The center walk passed using diagnostic trunk meshes; the final authored box colliders still require native proof. Target fitness is **8.3/10 PASS**, with the original kit, UI, avatar, terrain, lighting and camera authoritative over image-generated variations. The locked reference is `art/targets/heartwood-opening-v1/target.png`. This is a bounded first-circuit scenery improvement, not completion of the whole Heartwood habitat.

One production worker owns a focused `tools/compose-heartwood-opening.mjs`, its late hook in `tools/author-beta-world.mjs`, canonical `src/world/data/world.json` and generated sibling, plus focused composition/support tests. The composer owns only `prop_camp_heartwood_*` records; preserve every prior record, collection and order. Do not replay the older landscape composer alone: its dry run changes existing Camp and section1 overrides. Reuse the exact twenty admitted blockout records as the initial candidate. Four reachable trees use their existing scaled/yawed trunk box colliders; low plants remain nonblocking. Keep at least four metres of trunk-free lane and roughly three metres of readable low-floor lane, with natural leaf/crown overlap allowed. Preserve current Camp terrain/palette, build apron/gates, existing background props, all resources and Wildkin, protected frontier selection, budgets, state owners and save identities. No new assets, renderer, dependency, terrain profile, recipe or discovery.

Root owns same-camera integration, forward/return and side-trunk contact, source/visual review, the fresh useful-gathering/Camp payoff, exact original-save restoration and one aggregate package gate. Reuse the existing native/workbench/care proof where unaffected; do not replay every completed system. Source proof covers idempotent prefix-only composition, existing-content equality, full support and actual authored-mesh grounding, collider truth, and world/generated synchronization. At most three visual rounds; if the same gap persists, retain an honest hold and change the next approach. The broader first-circuit loop and all ten distinct habitat experiences remain unfinished until their own evidence exists.

Integration also identified an exact Windows file holder blocking generated-world replacement: the existing port8080 `tools/serve.mjs` process. Authorize a minimal response-stream lifecycle repair in that server, plus focused normal/aborted-response proof and restart of that confirmed local development server. Preserve file contents, atomic generation, ports, saves and browser state. Do not weaken file permissions or replace the atomic writer with an unsafe fallback.

The fresh native builder haul exposed the sibling copy gap: collecting enough berries/fiber immediately changed the plan back to a mandatory lure return. Extend the copy-only correction to the ready-to-craft branch: keep its ID, conditions, costs and priority, but offer Camp building and optional lure crafting, with an exploration-led return title. A player who deliberately already crafted a lure can retain the existing Mossling-finding guidance. No new progression or preference state.


Outcome and limits: [checkpoint receipt](../art/reviews/heartwood-opening/receipt.md). The whole Heartwood habitat remains unfinished.
