# September12 session handoff

**Current continuation — September13:** Fungal Hollow now has branching terrain, fuller mushroom scenery, four finite useful flowers and an avoidable aggressive Thornprowler. Ordinary controls proved raised-bank gathering, two supported routes, attack/escape and streamed depletion. **1,400 tests and package gates pass** (44.39 MB unpacked /20.59 MB ZIP), with the original earned Camp restored exactly in developer and portable builds. Visual R2 remains **6.5/10 HOLD**: the banks still read too flat in the ordinary camera. [Evidence](../art/reviews/fungal-hollow/receipt.md).

Next: review the opening Heartwood expedition from the ordinary Camp view, then scope one readable first circuit using existing forage, Mosslings and a useful Camp crafting/building payoff. Preserve the current kit and earned save; do not add a new species or framework before identifying the actual player-facing gap. Ten finished habitats remain required, and the continuous goal stays active.

The September12 stop below is historical; Chris subsequently authorized continuous work on main.

**Chris requested this stop for a PC restart and a fresh Codex session.** No active worker or required heavy job remains. Local dev/package servers may still be running until restart; they do not survive reboot. CURRENT_SLICE owns current scope.

## What is saved and playable

- Phone improvements: square Backpack cells, lifted drag preview, rounded panels, fullscreen entry, responsive landscape/portrait controls, continuous harvesting and persistent quiet observation/Journal clues.
- Survival loop: defended Camp yard expansion, physical storage/inventory, Survey cartridge fitting for16→20slots, Rootfall cutting/bracing and saved reciprocal travel, flexible five-slot Backpack shortcuts.
- Terrain: ordinary Jump and natural exploration replace active pads/checkpoint courses. Verified Verdant V7 retains Chris's accepted V3 rocks; selected Shatterfen V3 adds connected100×100m banks/grades and a northwest wreck supply route. Known visual debt remains documented.
- Latest camera: right-side horizontal/vertical orbit (20–64° pitch), pinch/wheel zoom, temporary terrain/solid retraction with gradual recovery. Trees fade; construction/defenses block camera placement while retaining gameplay ownership. Movement uses horizontal camera heading only.

The latest camera/source package passed966 tests, world/campaign, build/validation and ZIP:43.84MB/20.46MB. Dev/native proof includes touch ownership, terrain recovery, moving shoulder, Jump, portrait, canopy fade, built wall and Author isolation. Fresh packaged proof covers touch orbit, terrain clearance, Jump and literal Continue. Exact source/package hashes, screenshots and independent review are retained in art/reviews/gameplay-camera-v1/. No physical-phone comfort or uninterrupted visual-animation acceptance is claimed.

## Resume here

1. Restart the local server as described in SESSION_START. Preserve Chris's browser save; use a separate context for diagnostics.
2. Repair the **one remaining Emberfall west-descent/foundry slope merge** in the isolated composer. Follow EMBERFALL_CANDIDATE_HANDOFF, not a fresh broad redesign. Target8.1, gameplay ledger and baseline maps are already retained. The candidate is not wired into runtime; its opt-in test is3/4 passing.
3. Once the full-width route is sound, root integrates the canonical region serially and regenerates. Prove the high loop from the normal camera, including a companion, then judge actual top-down and native views against the retained target. Add restrained dressing only after the route works.
4. TOOL_PROGRESSION_NEXT contains the queued repeatable physical cutter proposal. It derives availability from the completed existing Survey chest claim, supporting older claimed saves and recrafting without consuming a unique blueprint or adding a new save ledger. It remains design, not implemented behavior.

Tidefin V3 remains unshipped. Preserve its exact GLB and sampled action review. Native speed calibration, attack/projectile timing, full Hurt/Attack playback and uninterrupted perception remain open. Drone, more wreck/blueprint progression, ranged tools/weapons/ammo, armor, further Camp sectors and other region/asset polish remain planned; do not claim they were completed.

## Workflow that worked

- The root acts as producer: choose one playable slice, name its state/file owners, delegate independent uncertainties, and integrate shared world/package changes serially. Terra High handled bounded code/plan work; Astra High handled the level/camera/visual judgments. No Ultra requirement is needed for routine tasks.
- Dream Loop for assets/regions/UI: inspect the actual baseline and approved Explorer master, have a reference author generate a target, independently reject AI inconsistencies or impossible geometry, implement against the locked target, then compare actual captures. A target score never admits runtime art or mechanics.
- Region planning uses clean/planning/oblique atlases, exact elevation and a role ledger. Props can contain resources and live Wildkin; keeping only resources/creatures arrays loses gameplay. Numeric IDs/roles override invented image details. Check real width/grades/encounter space and alternate returns, not only centerline arrows.
- Models use the established Asset Forge workflow: preserve source/albedo/UVs, bounded local image-to-3D when memory allows, deliberate Blender cleanup/reduction, neutral alignment, anatomical rig/weights, isolated exported clips, then native scale/timing. Do not lower safety guards or install another generator simply because an animation is held.
- Native proof uses one isolated Edge browser at a time. Label grants/warps/reloads, hash the exact sources, preserve failed receipts, and inspect screenshots. Small Node tests establish geometry/state contracts; actual in-game behavior and Chris's observations determine perceptual acceptance.
- At an integrated boundary, root runs verify/ZIP plus a short packaged smoke, updates current docs/build log, commits on main and stages one consolidated Brain Brief. Do not repeat the full matrix for every worker edit.

## Lessons and failed approaches

- Point overlap alone cannot tell whether a focus is below a terrain triangle mesh. Camera recovery now combines active terrain height, a solid overlap query and a sphere corridor from the current player focus. An upward-clearance heuristic incorrectly rejects valid low lintels; no roof-crossing fallback is used.
- Initial static collider snapshots omit player-built walls and Camp defenses. Explicit registration/removal in their existing lifecycle fixes the shared path. Scenic tree trunks remain gameplay-solid but camera-fade-only.
- Zero camera distance must retain an explicit look direction; lookAt at the camera's own point can corrupt movement heading. Collision distance must not overwrite requested zoom.
- A stale named-tree fixture failed because Verdant had replaced that prop. A first sampled camera point also overlapped scenic solids. Both failures were retained; the corrected current/capsule-clear fixture passed. Do not weaken gameplay constraints to satisfy a broken harness.
- A line's gentle center grade does not prove its full width where terrain routes intersect. This is exactly why Emberfall remains isolated at the stop.
- Imported generation scripts may return before their asynchronous write finishes. Wait for the generator child process to exit, then hash actual output. Do not use whole-world regeneration when only one region is intended unless parity is proven.
- Sampled stills and recorded-but-unperceived videos do not prove animation rhythm. Preserve Tidefin V3 and solve native timing before another model loop.
- Chris's bedtime best-of-three/four delegation closes aesthetic stalls through honest provisional selection, not inflated scores. It never waives collision, save correctness, resource safety or spending limits.

## Local recovery and limits

Work is committed locally on main; this wrap performs no push/deploy. The September11 remote backup is older. No reset credit or paid generation was consumed in the final camera/Emberfall pass. Keep existing TRELLIS memory guards and one-heavy-job discipline. The last phone URL was http://192.168.4.96:8080/; verify the current address after reboot. Tracked skills, sources, targets, candidates and evidence are the restart authority; ignored scratch is not required for this candidate's recovery.
