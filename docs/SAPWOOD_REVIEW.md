# Sapwood harvesting — September 11 candidate

Nine builtin harvest trees now use a distinct alien wood plant: five pale-grained woody ribs with attached teal fins and one matching permanent root. Five strikes still yield the same wood and regrow after eighteen active seconds. Decorative canopies, ancient trees and logs remain scenery; the separate eight-hit library Redwood keeps its own model and settings.

Root authored a full/depleted target, a Blender implementer reconstructed the model, and an independent reviewer inspected all seven actual exported views. Model V2 passes8.1/10, exact SHA256`7733a50f5bd4bfc01671bc802e73fcb44a78a0068168c8c4ec269bf7aa30c16b`:1,381 triangles, six meshes, one256px palette and108,456 bytes. The generated target's two roots did not precisely agree, and some pointed fins resembled crystals. Production uses one root with fixed joints and flattened blunt attached fins. Editable source and exact review are under `art/source/sapwood-v1/`; target/provenance are under `art/targets/sapwood-v1/`.

The shared resource path now removes named complete assemblies. Ungrouped library harvestables distribute their parts over their authored hit count. On depletion the complete ready visual hides, so old trunks/details cannot remain on top of the remnant. Sapwood reuses its exact modeled stump; other families keep their existing remnants. Each resource owns a small set of feedback materials while sharing model geometry/textures; strikes, tint and regrowth preserve sibling instances and authored opacity/emissive.

Ordinary-input browser proof at844×390 shows five F strikes removing five ribs, exact cut root, unchanged neighboring copies, actual eighteen-second regrowth and safe collider deferral while the player occupies the stump. Stepping away restores collision. Root reviewed full/front, rear and depletion frames. The rear has less pale wood detail than the front but retains its fan silhouette; a nearby offscreen extraction label can cover part of the plant. These are limited browser observations, not physical-phone acceptance.

Independent code review caught two reset gaps: extraction/new-run paths needed to restore the newly hidden ready root, and depleted nodes reset while their region was inactive could return without collision. Both main reset paths now call one resource-owned reset, which retains pending collision restoration until the region resumes and the player is clear. Tests cover real Rapier occupied and inactive restoration, nested chunk assemblies, material isolation, legacy proportional depletion and Author-hidden visibility. Native evidence is under `.dream-loop/overnight-sapwood-runtime/`; its final report owns Author/package proof and remaining limits.

Final Author proof changes this resource's scale to1.1, saves/reloads and returns to Play: the actual GLB, five chunks, modeled stump, scale and collider agree. Packaged8081 loads both new local models, repeats native Sapwood depletion and chest opening, then supports already-loaded interactions with the browser offline. A final local-server reload retains claimed-open state while external hosts remain blocked. Zero external requests or page errors. This is not a claim of reloading the app shell with localhost itself disconnected.

For a player check, enter Verdant Verge and find the pale fan of wood beside the berry bush near the first path. Turn off Auto to inspect individual strikes. Strike five times: each hit should remove a whole rib and its fins, finally leaving only the low cut root. Stand on the root until the plant returns, then step away. It must not shove or trap you, and the trunk should become solid after you clear it. Return to Camp and depart again: the plant should be visible and solid normally. Orbit behind it and report if it looks like an unharvestable decoration.
