# Emberhorn focused R2 mane plan — independent review

**Status: PASS to the separate, mane-only R2 builder.** JSON SHA-256 matches
`7a45176b2e4c636abe6649f122ec2d6bcf5a3342160e3d076821a3e796aab2c4`.

The repair directly addresses the held R1 failure. Each existing mane piece now
has three ordered, convex five-point profiles: a lower/narrow front end, a
wider raised middle and a lower/narrow rear end. Their literal Z intervals
produce row overlap (rear `[-.36,0]`, middle `[-.10,.26]`, front `[.17,.53]`),
and 0.135m column spacing overlaps the 0.22m middle sections. The 0.36m
per-wedge depth, buried base height band, declining outer/rear peaks and
centre high point give a credible compact brow-to-neck ridge instead of R1's
common-top rail stack.

The construction is executable: bridge like-indexed vertices across the three
five-point profiles, cap only the two convex ends, and retain closed outward
surfaces. Computed mane bounds `[-.38,.8,-.36]..[.38,1.36,.53]` remain inside
the preserved creature envelope and below the horn tips. The JSON preserves
all R1 leg coordinates and limits repair scope to the fifteen named mane
meshes; body/head/horns/hooves/materials/gameplay and sibling species stay out
of scope.

Builder must direct-load the frozen literal coordinates, prove R1 limb/hoof
hashes and all unchanged named occurrences exact, then audit every revised
wedge for closure/outward normals/positive volume before matching neutral and
48/96px renders. This is a method pass, not visual admission; actual review
must still reject exposed bases, a bar/step silhouette, or a crest that
vanishes at phone scale.
