# Thornprowler rotation 1 — independent anatomy plan review V1

## Decision: PASS to separate builder

The frozen input hash matches `FC8FD1107F53D624E9DD6770E637C41942E58AE9D03D06D77256013D4BF795A7`. It stores four literal 32-vertex, four-ring legs and seven six-base-pyramid thorns rather than relying on prose geometry. The planned 770 triangles across the existing 23 meshes is compatible with the narrow scope.

Each paw and torso endpoint cap has all eight ring points checked against both shell intersections, with a common interior interval and positive overlap; each of the 42 thorn-base corners likewise has measured torso-shell evidence. The computed changed-parts envelope remains inside the frozen original envelope. The planned bent, widening leg paths address the target's thin, hanging-rod defect while retaining a crouched profile, existing paws, and exactly seven thorns.

The existing code-native closed-ring/loft helper is an appropriate implementation path provided the builder consumes the literal plan values and audits directed shared-edge orientation, volume, endpoints, protected component parity, full proposed bounds, and actual 48/96 px neutral contour. The source change alone cannot admit the resident: after independent visual retention, root must run the focused Thornprowler bake and verify source → `world.json` → generated data → actual factory, while retaining all unselected records, gameplay fields, collision, siblings, and the existing Fungal Hollow lifecycle.

This approves only the initial geometry candidate. It does not authorize a behavior, collider, habitat, or runtime change.
