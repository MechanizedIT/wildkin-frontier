# Physical alien discoveries — September 11, 2026

The existing Emberhorn secret is now a rock-armored forge vault. Calling Cragbreaker nearby separates the two shutters, then extends a supported reward tray. Collecting its existing reward removes the core from that tray. The same secret ID, once-only claim and expedition cargo/XP ownership remain authoritative.

This model production loop and its bounded runtime review pass. The target author, Blender implementer and model judge were separate agents. V1 shape HOLD7.3 led to a fractured housing/ivory armor revision; V2 shape passes8.0. Independent opening readability also scores8.0 in the actual 844×390 scene. See `art/source/ember-forge-cache-v1/root-review.md` for exact hash, budget, source and limitations.

The one existing game loop owns the presentation. Static visual roots are looked up once; per-instance node transforms remain independent. The .515m shutters clear before the .25m tray movement. Pause freezes parts, Author restores the neutral model, and section changes/reload derive completed state from saved seals/claims. Reduced motion snaps to the final visible state.

Native 844×390 review uses a disclosed secured Emberhorn/waypoint/start fixture, followed by ordinary Q, movement, Pack, context taps and reload. It verifies front/side blocking, visible part changes, exactly3 iron and78 XP once, and empty/open reload with no browser errors. The first rapid touch sequence exposed a real defect: OPEN was actionable before the reveal finished. The corrected replay rejects raw taps, E and direct authoritative access while OPENING, then allows one claim after the tray finishes. Reduced motion snaps ready; focused Author preview restores the closed model and correct Solid/scale settings. Evidence is retained in `.dream-loop/overnight-ember-cache-runtime/`. The Explorer can obscure the tray from directly in front; an orbit makes its travel clearer. This is not a fresh-save campaign or physical-phone performance result.

Solid loot is opt-in through `collisionEnabled: true`; existing non-solid loot remains unchanged. Its referenced asset descriptor supplies the same scaled/rotated/offset box in Author and Play. The new vault opts in and has a1.65m interaction radius. Focused tests prove rotation/scale/elevation, JSON normalization, runtime obstacle parity and the explicit Solid toggle. Other generic chests, parkour rewards, the Core, and companion seals preserve their previous default collision/access rules.

Human check: travel to Emberfall with Emberhorn selected, follow the western return past the ore toward the ivory-doored vault near the beacon. Use Cragbreaker, watch the shutters separate and tray extend, then approach and collect. OPENING should prevent early collection; afterward the center is empty. Try walking against its front and side, then return on another expedition: the vault stays open and cannot pay twice. Failure signs are a floating or disappearing shutter, a reward before the tray moves, walking through the shell, or another reward after returning.

Other species' seals and generic containers still need their own distinct physical mechanisms. The surrounding foundry terraces remain an explicit art HOLD despite passing movement/support checks.
