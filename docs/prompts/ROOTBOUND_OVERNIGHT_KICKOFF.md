# Kickoff prompt — Rootbound habitat + one native Wildkin overnight trial

Paste the text below into a fresh Codex session opened at the Wildkin Frontier repository.

---

Resume Wildkin Frontier from the September 13 workflow-review stop. This prompt is fresh owner direction and authorizes one bounded unattended production run. Work directly on `main` under the repository rules. Commit cohesive integrated batches locally. Do not push unless I separately authorize it in this session.

## Read first

1. `AGENTS.md`
2. `docs/SESSION_START.md`
3. `docs/CURRENT_SLICE.md`
4. latest handoff
5. `.agents/skills/wildkin-development/SKILL.md`
6. `.agents/skills/overnight-orchestrator/SKILL.md`
7. `.agents/skills/habitat-development/SKILL.md`
8. `.agents/skills/wildkin-species-development/SKILL.md`
9. `docs/OVERNIGHT_PRODUCTION.md`
10. `docs/habitats/rootbound-wildwood/PACKET.md`
11. relevant `CODE_MAP`, architecture, asset-pipeline, and current-slice sections only

Confirm actual Git status, current commit, running tools/processes, and any unrelated dirty work before editing. Preserve unrelated experiments.

## Product priority

The game is portrait-mobile-first. Exploration is first priority, collecting Wildkin is second, and building/crafting/deeper Wildkin systems are third. The early-alpha world is one large finite continent with ten unique habitats, not an infinite procedural world.

## Operating mode

Act as one root orchestrator. Use subagents for disjoint bounded work, not a single agent doing everything.

The project config allows up to six spawned threads when available, but use capacity deliberately:

- no more than three write-capable agents at once;
- one writer per file or authoritative domain;
- reserve at least one independent reviewer;
- one TRELLIS/Blender/heavy-GPU task at a time;
- root owns canonical integration, shared data/schema, CURRENT_SLICE, commits, and aggregate verification;
- do not run multiple agents controlling the same browser or Blender instance simultaneously.

Do not run an endless loop. Complete at most three integrated batches, then stop with a morning handoff.

## Batch A — Rootbound Wildwood structural improvement

Use the habitat packet and habitat-development skill.

Goal: make one repeatable 3–5 minute Rootbound circuit stop reading as a flat evenly scattered alien meadow and start reading as a designed wildwood with terrain rooms, route choices, clustered ecology, scale hierarchy, and a landmark.

Requirements:

- capture actual matching baseline overhead plus arrival/interior/destination portrait views;
- freeze a bounded route and protected content;
- first change terrain/negative space/room structure, not simply instance count;
- establish at least an open orientation clearing, root-gallery passage, shallow fungal destination pocket, and concentrated thornstone seam or equivalent feasible interpretation;
- establish a visible Heartroot Crown landmark or honest bounded proxy;
- preserve existing resource/Wildkin identities, save ownership, shared terrain/collision, and Camp/Heartwood work;
- perform one structural implementation round;
- use an independent habitat judge with the packet scorecard;
- perform one focused repair round based on the three most consequential gaps;
- checkpoint after that even if the result remains HOLD;
- do not spend the whole session nudging this habitat toward a score.

Ordinary play must walk the route and exercise one useful Rootbound interaction. Verify literal reload for any changed persistent state.

## Batch B — one Rootbound-native Wildkin candidate

Run this beside Batch A only where ownership and tools are disjoint. Use the wildkin-species-development and wildkin-asset-forge skills.

Start with three concise species concepts tied to Rootbound’s actual exploration needs. Select the strongest feasible concept independently and freeze a species card.

The species must:

- have a distinct body/silhouette, habitat niche, temperament, bonding behavior, and exploration utility;
- not duplicate Mossling healing/grove use, Tidefin Ward, or Emberhorn breaking/combat;
- make dense exploration, ecological reading, route discovery, or a specific Rootbound risk meaningfully different;
- remain optional rather than a universal required key;
- use only traits the art/runtime can actually express.

Split visual asset and gameplay design work:

- Visual lane: approved reference → reference review → one serialized TRELLIS 2 candidate → Blender cleanup/inspection when feasible → gameplay-scale renders → independent visual review.
- Gameplay lane: behavior/taming/utility contract and, only if safely bounded, a placeholder prototype through existing encounter/capture/save owners.

Two substantial visual rounds maximum. Do not force integration if the model, motion, behavior, or habitat is not ready. A reviewed species card/reference/GLB candidate is an honest useful checkpoint.

A species batch may request at most one or two supporting Rootbound flora/resource model candidates from TRELLIS if they are necessary for readability. Do not turn it into a biome asset dump.

## Batch C — optional enabling improvement

Begin only after A or B reaches a coherent checkpoint.

Choose at most one:

- measured streaming/scenery improvement on the actual Rootbound route;
- a small reusable habitat patch/inspector tool;
- one targeted flora/resource/landmark asset required by the packet;
- one small gameplay contract required by the chosen species.

Do not add weather, caves, another habitat, broad genetics, factory systems, or a new framework.

## Dream Loop and review rules

Use actual game baselines and feasible targets. Generated art is direction, not proof.

- Default two rounds.
- A third is allowed only for a designated hero surface and only after changing the structural method.
- Implementers cannot independently PASS their own visual work.
- Reviewers return three consequential gaps and one consolidated repair packet.
- If the same structural gap repeats twice, stop that method and record HOLD.
- Human/mobile perceptual observations outrank implementation metrics.

## Testing and closure

Use focused tests while developing. Do not rerun the entire aggregate after each visual round or docs-only edit.

At each integrated checkpoint:

- complete relevant ordinary-input play;
- verify persistence/reload when changed;
- run focused tests;
- run one aggregate `npm test`, `npm run verify`, and `npm run zip`;
- record actual package/performance status without claiming physical-phone proof;
- commit the cohesive batch directly to `main`.

## Stop conditions

Stop and hand off if:

- a consequential design choice needs me;
- repeated structural failure exhausts the loop budget;
- save/physics/collision/data ownership becomes uncertain;
- GPU/RAM/tool instability threatens the machine;
- independent perceptual review is unavailable for an admission decision;
- three integrated batches are complete.

## Morning handoff

Leave one concise illustrated checkpoint with:

- what changed for the player;
- actual current images and how to open/try the work;
- PASS/HOLD per lane;
- exact commits and test/package status;
- admitted versus provisional candidates;
- honest defects and untested claims;
- three recognizable things for me to try;
- the next recommended batch.

Do not auto-start another overnight cycle after the handoff.
