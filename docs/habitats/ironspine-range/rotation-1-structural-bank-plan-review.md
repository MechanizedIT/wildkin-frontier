# Ironspine rotation 1 — independent arrival-bank plan review

## Decision: PASS for one narrow structural source pass, with target HOLD retained

The native pair preview makes the proposal credible as a small arrival framing improvement. The two supported `asset_verdant_cliff_buttress` transforms create a readable left-side vertical edge while retaining a clear central player lane. The full mesh and hull projections put both members in the useful portrait region, and the support data records whole-hull terrain support plus source, home, solid, and route clearance. The narrowest stated route margin is 1.3198m.

This is not the selected target's canyon room. The preview still leaves the right side and route depth open, and the two assets are separate scenery records rather than a continuous terrain bank. “Connected left gully edge” must therefore mean an adjacent visual cluster only; it must not imply joined terrain, new collision, or a completed gully.

The bounded source pass may add exactly the two documented stable default-world scenery records in chunk `(-42,-64)` using the existing Skybreak landform lifecycle. It must retain all terrain, camera, route, source/home footprints, existing records, renderer systems, resource systems, and alternate-world output. Before visual acceptance, root must prove resident lifecycle/unload-reentry, exact default/alternate-world seams, collider and close-pass clearance, matching arrival/interior/destination frames, and the ordinary circuit. The preview is diagnostic only and does not replace those proofs.

If the actual view loses the left framing or makes the route feel pinched, close this visit HOLD rather than adding unplanned buttresses or terrain. A later repair would need a changed method, not a further placement search.
