# Emberglass Caldera structural plan V2 — independent evidence review

## Decision: HOLD — close this measured shoulder method without source or preview

The finite portrait-surface finding remains enough to hold the present method: target-scale shoulder space is concentrated in the protected corridor-side strip, and the three tested pairs do not have complete final support, ecology, or parity evidence. No candidate is eligible for a native preview or source pass.

The executed proof does **not** establish that the reported route-footprint failures were introduced by candidates A/B/C. It contains each candidate's final max/failure count but no same-grid, same-route, same `.32 m`/16-angle baseline result or explicit final-minus-baseline failure set. The `.879–.917` maxima may include pre-existing wide-corridor or gather-leg failures. Consequently, the plan must not say that each candidate "introduces" the listed failures, and the before-ID enumeration is not after-parity proof.

This does not rescue a candidate: the absent same-baseline delta, final full-support proof, affected-procedural-scenery after comparison, and native value proof remain blockers. Record the correct conclusion as: the candidates are **unproven and ineligible**, not proven regressions. The route/space intersection and the absence of a fully qualified target-scale candidate close this visit's shoulder method; do not start another unmeasured nudge.

The ordinary unchanged baseline journey (`187.25 s`, `367.805 m`, four crystal shards, no iron, health `5 → 3`, exact reload) is useful current evidence but is not an accepted successful mineral circuit or terrain-change proof. It does not change this source HOLD.

## Correction after executed-script inspection

This measured HOLD cannot close the method yet. The executed script does not evaluate the declared right lobe: candidate rows are `[id,lx,lz,lrx,lrz,lpeak,rx,rz,rrx,rrz,rpeak]`, while its right-lobe call reads the wrong positions, effectively treating left peak as a right center coordinate and shifting the remaining parameters. It also tests radial height differences across `.32 m`, not the gradient of the final rendered 2 m triangles used by the player/physics path. Along with the missing same-baseline comparison, those defects invalidate the three-candidate rejection numbers.

The intended three finite parameter sets may be rerun with corrected parameter unpacking, final-triangle gradient/footprint sampling, and exact baseline-versus-final failure sets. That is proof repair, not a new candidate search or an authorization to preview/source-mutate. Only the corrected evidence can support a measured terminal HOLD.

## Current corrected-proof recheck

The receipt now compares same-route baseline and candidate samples and uses corrected right-lobe parameter positions plus a triangle-gradient predicate. It is still not terminal evidence: final terrain values are added in JavaScript double precision, while the runtime terrain mesh uses Float32 vertex heights; the script needs `Math.fround` at the final vertex value before interpolation/gradient. The narrative/table must also be rewritten to match the current receipt (`374 → 516/506/454` final failures and `68/66/40` introduced failures), replacing its obsolete `202/191/194` claims.

Until that precision-aligned rerun and coherent plan receipt exist, do not close a measured candidate HOLD. The corrected method may be rerun for the same three frozen candidates only; it does not authorize source work or a new search.

## Terminal Float32 re-review

### Decision: PASS — close these three finite hypotheses as HOLD

The public proof uses the corrected candidate right-lobe indexes, `Math.fround` final vertex values before actual 2 m-triangle gradient evaluation, and a same-route/same-footprint baseline set. The results establish introduced failures rather than merely reporting high final values: A `374 → 516` with 68 introduced, B `374 → 506` with 66 introduced, and C `374 → 454` with 40 introduced. First introduced samples at `(848,-1966)` are above the `.32` limit for all three.

The pre-existing baseline maximum and 374 failures remain a different full-width/gather-branch condition than the earlier center-footprint survey; they do not erase the distinct candidate deltas. The absent after-ecology parity is correctly non-claimed because every candidate is already ineligible on route support before source work. This closes only A/B/C and does not assert that Emberglass, other terrain methods, or future camera/placement evidence are impossible. No native preview or source change follows.
