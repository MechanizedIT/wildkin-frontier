# Sunscar rotation 1 structural plan — independent pre-runtime review

**Decision: HOLD for four bounded corrections before source work.** The plan has the right narrow asset role and preserves the target's low lane-edge intent, but its current numerical report cannot admit either anchor.

1. **Use Three.js yaw consistently.** The probe currently maps local coordinates as `x = u*cos - v*sin, z = u*sin + v*cos`. A Three.js `rotation.y` transform for the stated local long/depth axes is `x = u*cos + v*sin, z = -u*sin + v*cos`. Change both planform sampling and box projection to that mapping; change `pointRect` to the matching inverse (`u = dx*cos - dz*sin`, `v = dx*sin + dz*cos`) before any reported support, clearance, or screen bound is trusted.
2. **Sample the complete transformed planform.** The `.5` loops never reach `u=+2.2` or `v=+1.05`, despite the 4.4 × 2.1 m envelope. Generate clamped coordinates that include both extrema and all four exact corners, then resurvey support, protected clearance, and projections. Do not retain the present “45 points/full planform” claim.
3. **Replace the generic protected radius with measured shapes and approach buffers.** On the current calculation the destination candidate's nearest source is 4.288 m from the rectangle while the code's own `(1.6 + 3.0)` test marks it protected, making `freeStandingPass` false. Inspect the actual full transformed source footprint/collider and state a separate bounded approach lane buffer; apply both against the corrected rib planform. Do not waive this by changing a constant or assume a point source has a 1.6 m footprint.
4. **Remove the current arrival-right anchor and repair route language.** Its approximate arrival box is `x=-115..35`, so at most a sliver is in the useful portrait frame and it cannot provide the promised basin echo. Keep it rejected rather than calling an offscreen form a direction. The declared loop also runs directly through the Emberhorn home at `(-1930,28)` while the packet promises a safe western skirt versus a richer central decision. Either record that line as the deliberate central-risk branch and add a measured west-skirt branch, or remove the safe-skirt claim; do not call the current polyline the safe route.

After these corrections, proceed only if the destination anchor passes corrected full-mesh support, source/home/approach clearance, and native portrait visibility. A single visible destination rib is preferable to counting an offscreen arrival copy. This review does not approve terrain edits, collision, asset build, or source placement.

## R2 provisional native-projection gate

**GO for bounded native full-mesh projection only.** The R2 script now uses the Three.js-compatible transform and inverse, includes the full 4.4 × 2.1 m extrema in 60 support samples, uses the west-skirt route, and applies a declared 1.2 m source-approach and route-shoulder rule. The two named rows are analytical candidates, not runtime admissions: destination `(-1942,83,0.25)` has 7/8 central box corners and arrival `(-1986,-7,0.25)` has 6/8. Both need the delivered mesh’s full transformed bounds and native capture before any visual or placement decision; the arrival cap remains close to the HUD boundary.

One factual correction is required in the report/plan before later handoff: the executed grid loops produce **472** rows (`10 × 8 × 2 + 12 × 13 × 2`), matching `gridCount` in `planning-rib-probes-r2.json`, not the amendment’s stated 448. This does not invalidate the two candidates, but the analytical evidence must not misstate its sample population.
